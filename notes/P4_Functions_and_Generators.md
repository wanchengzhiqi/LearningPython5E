# P4 Functions and Generators

本文件记录 `P4_Functions_and_Generators` 的阶段学习成果。P4 从“会写控制流”进一步进入“如何定义可调用边界、传递对象、组织结果与表达合同”的语言主线。当前收录已经形成稳定证据的 `C16_Function_Basics`、`C17_Scopes` 与 `C18_Arguments` 阶段成果；各章是否最终关闭，以其阶段状态段和生命周期证据为准，C19 及后续章节不在这里提前展开。

---

## 18. C16 函数基础：函数对象、调用时间线与合同边界

### 18.0 阶段状态和可追溯入口

- **章节角色**：`P4_Functions_and_Generators` 的 **PART opener**。
- **唯一教学路线入口**：`docs/C16_FUNCTION_BASICS_STARTUP_TEMPLATE.md`。
- **正式主线工件**：
  - `01_def_function_objects_and_aliases.py`
  - `02_definition_call_and_return_timeline.py`
  - `03_return_paths_none_and_exceptions.py`
  - `04_return_values_and_side_effect_contracts.py`
  - `05_docstring_signature_and_annotation_evidence.py`
- **阶段测验**：`stage_quiz_function_basics.md`，最终得分 **99.75 / 100**；共 6 个分区、11 题，结构与 41 个 Python 代码块均已通过验证。
- **项目关卡**：本章没有由 active-gate authority 明确排期的测验前 capstone；路线图中的候选项目不能据此升级为本章必做项目。
- **收束状态**：有限主线、正式工件、阶段测验、学习画像、阶段末笔记与最终收束均已完成；C16 于 2026-08-03 标记为 `closed`。下一章从 `docs/C17_SCOPES_STARTUP_TEMPLATE.md` 另开新会话，不在这里提前开始 C17。

本章承接 P3 已经建立的对象模型、名字绑定、原地修改与重新绑定、返回值与副作用、输出流和证据边界，但不重抄旧结论。C16 的新增任务，是把这些基础统一到一个可追踪的函数模型里：**定义时发生什么、调用时发生什么、函数如何退出、调用者最终能观察到什么，以及各种证据究竟能证明到哪一层。**

### 18.1 PART opener 承接、有限阶段地图与本质模型

本章真正目标不是记住 `def` 和 `return` 的拼写，而是建立一条可以逐节点解释的时间线：

```tex
源码中的 def
→ 解析与编译形成代码相关产物
→ 运行到 def 语句
→ 创建函数对象并绑定名字
→ 求值调用表达式的实参
→ 建立本次调用的局部绑定并执行函数体
→ 正常 return 或异常退出
→ 调用表达式产生结果或向外传播异常
→ 调用方后续语句才可能继续
```

有限主线只覆盖六层：

| 层次 | 必须回答的问题 | 本章结论 |
| --- | --- | --- |
| 定义 | 执行 `def` 是否会执行函数体？ | 不会；它创建函数对象并绑定定义名称 |
| 对象 | `function` 与 `function()` 是否相同？ | 前者取得函数对象，后者发起调用并形成调用结果或异常 |
| 调用 | 实参如何进入函数？ | 先求值，再把对象引用绑定给本次调用的形参局部名字；基本调用不自动复制对象 |
| 退出 | 函数怎样结束？ | 显式 `return`、裸 `return`、运行到末尾，或异常退出 |
| 合同 | 调用者能观察到什么？ | 返回对象、传入对象的修改、输出流写入、外部状态变化与异常是不同通道 |
| 证据 | 文档、签名、注解和一次运行能证明什么？ | 各自只提供有限证据，不能单独等同于完整运行合同 |

本章的必学核心是上表；必要补救集中在四个旧风险：

1. `function` 不是 `function()`；
2. 形参绑定不是“把实参对象复制进函数”；
3. `print()`、原地修改和持久化不是 `return`；
4. `callable()`、签名绑定成功或注解存在，都不保证真实调用成功。

函数属性的完整目录、栈帧、字节码、闭包、高阶函数、完整 LEGB、完整参数规则和生成器都不是本章必学范围。它们即使曾被追问，也只能作为解释当前模型所需的有限拓展，不能改变主线完成标准。

### 18.2 `def` 执行：函数对象、名字绑定、别名与重新定义

遇到函数定义时，要分清三个时间点：源码被解析和编译、控制流执行 `def` 语句、未来某次调用执行函数体。最基本的观察如下：

```python
events = []

def record():
    events.append("body")
    return "done"

print(events)       # []：定义时函数体没有运行
alias = record      # 同一个函数对象的新引用
print(alias is record)  # True
print(record())     # done：这里才执行函数体
print(events)       # ['body']
```

应知应会：

- `def` 是可执行语句。控制流运行到它时，Python 创建函数对象，并让定义名称绑定到该对象。
- `alias = record` 复制的是对象引用，不是函数体，也不创建新的函数对象。
- 把函数对象放入列表、字典或作为另一个名字的值，仍然只是建立额外引用。
- `record` 是函数对象；`record()` 是调用表达式。调用结果可能与函数对象完全不同，也可能根本没有正常结果而是传播异常。
- 函数体内的副作用、异常和返回路径只会在实际调用且执行到对应语句时发生。

同名的第二个 `def` 会创建新的函数对象，再改变该名字的绑定；它不会“就地修改”旧函数：

```python
def label():
    return "v1"

old = label

def label():
    return "v2"

print(old(), label())                 # v1 v2
print(old is label)                   # False
print(old.__name__ == label.__name__) # True
```

这里的关键边界是：

- `old is not label` 说明两个名字当前指向不同函数对象；
- `old.__name__ == label.__name__` 只说明两个对象记录了相同的定义名称字符串；
- 元数据相等不推出对象身份相同；
- 旧函数没有被修改或销毁，只要 `old` 等引用仍存在，它就仍然可达、可调用。

此前作答已经准确抓住这一点。唯一需要固定的书写细节是特殊属性名应写成 `__name__`，而不是被 Markdown 加粗语法误显示成 `**name**`。

### 18.3 调用边界：实参对象、形参局部绑定、修改与重新绑定

一次基本调用可以先用下面的有限模型理解：

```tex
求值实参表达式
→ 把所得对象引用绑定给本次调用的形参局部名字
→ 执行函数体
```

这不等于“按值复制整个对象”，也不等于“把调用方变量本身传进去”。函数收到的是对象，形参是本次调用中的局部名字。于是要继续沿用 P3 的对象模型来区分修改和重新绑定：

```python
def update(items):
    original = items
    items.append("body")  # 修改共享列表对象
    items = ["local"]     # 只重新绑定局部名字
    return original

records = ["caller"]
result = update(records)

print(records)          # ['caller', 'body']
print(result)           # ['caller', 'body']
print(result is records)  # True
```

精确解释：

- `items.append("body")` 作用于形参与调用方名字共同引用的列表对象，因此调用后可从 `records` 观察到修改。
- `items = ["local"]` 只是让本次调用中的局部名字改指向新列表，不会自动改变 `records` 的绑定。
- `original` 在重新绑定前保存了原列表引用，因此返回的仍是原列表，`result is records` 为真。
- “函数内改了变量”太含糊。必须说明是**修改了某个对象**，还是**重新绑定了某个名字**。
- 对不可变对象执行产生新值的运算，通常表现为得到另一个结果并重新绑定名字；不要把它描述为原对象被原地改变。

工程上，若函数会修改调用者传入的可变对象，应在名称、文档或接口设计中明确表达；若希望数据变换更易组合和测试，优先返回明确结果，并谨慎决定是否同时承担原地修改。完整参数匹配规则属于 C18，本章只保留“实参求值 + 本次调用局部绑定”的基础模型。

### 18.4 函数退出：返回路径、不可达代码、异常与调用方赋值

正常退出和异常退出必须分开：

| 函数路径 | 调用表达式的正常结果 | 调用方后续行为 |
| --- | --- | --- |
| `return value` | `value` 求值得到的对象 | 可继续执行并用于赋值、传参或组合 |
| 裸 `return` | `None` | 正常继续 |
| 执行到函数体末尾 | `None` | 正常继续 |
| 抛出且未在函数内处理的异常 | 没有正常返回值 | 异常向外传播，正常路径被中断 |

重要边界：

- `return` 一旦执行，本次调用立即正常结束；同一路径中它后面的语句不可达。
- 裸 `return` 与自然落到末尾都正常返回 `None`，但这不意味着异常退出也“返回了 `None`”。
- 调用方赋值如 `result = risky()`，只有在右侧调用正常产生结果后才完成名字绑定。
- 若右侧传播异常，赋值语句没有完成；名字可能保持旧绑定，也可能在此前从未绑定。
- 异常会改变正常控制流，可能继续向外传播、展开调用栈，并触发适用的 `finally` 清理；不能把它缩写成“只改变控制流”。
- Python 不会因为异常自动回滚此前已经完成的对象修改、输出或外部写入。

```python
events = []
result = "previous"

def risky(target):
    target.append("entered")
    raise RuntimeError("boom")

try:
    result = risky(events)
except RuntimeError:
    events.append("caught")

print(result)  # previous
print(events)  # ['entered', 'caught']
```

这里 `"entered"` 不会因异常消失，因为 `append()` 已经完成了对共享列表的原地修改；`result` 保持旧值，是因为包含调用的赋值语句没有完成。此前对此题的推理是准确的。更精确的工程结论是：如果操作包含多个副作用步骤，就必须单独设计验证、补偿或事务边界，不能把异常机制误当成自动撤销机制。

### 18.5 函数合同：返回对象、参数修改、输出流与外部效果

评价一个函数时，至少分开观察四种通道：

```tex
函数合同
= 返回对象
+ 对传入对象的可见修改
+ 输出流写入
+ 文件、网络、数据库等外部状态变化
+ 可能传播的异常
```

它们之间不能相互替代：

| 现象 | 能直接证明什么 | 不能直接证明什么 |
| --- | --- | --- |
| `return report` | 调用表达式正常得到 `report` 所指对象 | 已经显示或保存 |
| `print(report)` | 向指定输出接收者发起格式化写入；`print()` 自身返回 `None` | 业务结果就是 `None`；内容已经持久化 |
| `target.append(x)` | 目标列表对象已被原地修改 | 函数返回了该列表 |
| `stream.write(text)` | 向该接收者发起写入；返回值通常是写入字符数 | 接收者一定是磁盘文件或用户一定看见内容 |
| `StringIO.getvalue()` | 当前内存文本缓冲区包含什么 | 内容已落盘、已刷新到设备或具有事务保证 |

本章最重要的工程习惯是让核心计算和边界效果可分离：

```python
def build_report(records):
    return {"count": len(records)}

def print_report(report, stream):
    print(f"count={report['count']}", file=stream)
```

这样可以分别验证结构化返回值与具体输出接收者的状态。`StringIO` 很适合做内存中的可观察接收者，但它只证明缓冲区状态；若换成终端、磁盘文件、网络包装器或自定义对象，刷新、持久化、失败方式和位置语义都由具体接收者决定。

阶段测验唯一扣分点正位于这里：曾把 `StringIO` 上观察到的“内容和位置变化”推广成所有 `print(..., file=...)` 兼容对象的通用保证。精确修复是：

1. 函数或 `print()` 负责**发起若干写入操作**；
2. 接收对象的具体协议与实现决定每次写入产生什么效果；
3. 可读内容、当前位置、刷新、持久化和可见性都不是抽象“可写对象”自动承诺的统一结论；
4. 多次 `write()` 不构成原子事务，中途失败时可能已留下部分效果。

因此，工程合同应明确：谁负责计算，谁负责展示或保存，调用是否修改输入，失败时可能留下哪些部分效果，以及哪些职责明确不属于该函数。

### 18.6 函数元数据、签名、注解与有限证据

“元数据”是对描述对象之信息的用途分类，不等于“函数对象所有前导和尾随双下划线属性的值”。许多双下划线属性属于 Python 数据模型接口；其中有些可视为描述函数的元数据，有些承担运行协议或实现联系。不要用拼写形式替代语义分类。

本章涉及的典型属性可这样理解：

| 属性 | 主要意义 | 形成或求值时机的关键点 |
| --- | --- | --- |
| `__name__` / `__qualname__` | 定义名称及限定名称 | 相关信息由编译产物提供，执行 `def` 创建函数对象时关联到对象 |
| `__doc__` | 函数文档字符串或 `None` | 文档字符串作为函数源码结构的一部分被编译；创建函数对象时成为其文档属性 |
| `__code__` | 已编译的函数体代码对象 | 在外围代码编译过程中形成；执行 `def` 时连接到新函数对象 |
| `__defaults__` | 已求值的位置参数默认值元组或 `None` | 默认值表达式在每次执行 `def` 时求值，然后交给新函数对象 |
| `__annotations__` | 注解映射 | 时机依 Python 版本与注解机制而异；不能笼统说所有注解都在 `def` 时立即求值 |

必须分开“编译源码”和“执行定义”：

```python
def sample(value: str, suffix="x") -> str:
    """Return a labeled value."""
    return value.strip() + suffix
```

- 解析和编译会检查语法、形成函数体代码对象以及定义所需的常量和名称信息；函数体不会因此执行。
- 函数体本身的语法错误、重复参数等静态非法结构会在编译阶段阻止这段代码正常产生可执行结果，即使未来从不调用该函数。
- 控制流真正执行 `def` 时才创建函数对象，并求值默认值表达式。默认值表达式此时若触发 `NameError` 等异常，本次 `def` 就不会完成相应名称绑定。
- 函数体里引用的普通名字通常不要求定义时已经有值；只要语法可编译，可能直到调用并执行到该路径时才发生 `NameError`。
- 在本章当前解释器 Python 3.14.5 的默认注解语义下，函数注解采用延迟求值机制：定义时可先建立延迟求值入口，随后访问 `__annotations__` 等操作才可能触发注解表达式求值。这是版本相关事实，不能倒推旧版本或不同注解配置的行为。

文档字符串、签名和注解提供的是不同维度的证据：

- `callable(obj)` 只说明对象可被调用，不说明某组实参会成功，更不说明业务结果正确。
- `inspect.signature(func)` 显示当前可见的调用形状，是运行时内省证据。
- `signature.bind(...)` 只检查这组参数能否按签名形状绑定；它不会执行函数体，也不会验证业务语义或注解类型。
- 注解表达设计意图，Python 默认不会仅因 `value: str` 就阻止传入整数。
- 文档字符串说明作者声明的用途和约定，不是运行成功证据。
- 一次成功运行只证明该具体输入和环境下的观察结果，不能独自证明所有路径的完整合同。

例如，`sample(404)` 的参数形状可以绑定，注解也仍可读取，但真实调用会在求值 `value.strip` 时确定地触发 `AttributeError`：失败点是**属性查找**，甚至还没有进入 `strip()` 方法调用。对给定代码、输入和 Python 3.14.5 环境，这个异常类型是确定事实；“实验范围有限”不应被误写成该次结果本身“不确定”。

本节来自学习中的高质量追问，属于可选拓展而不是当前目标修复：它加深了定义时间线与证据边界，却不把字节码、描述器或完整反射协议加入 C16 必学范围。

### 18.7 正式工件的工程价值与相邻背景边界

五个正式脚本不是互相孤立的语法演示，而是一组逐层增加观察点的最小实验：

| 工件 | 主要设计价值 | 受控边界 |
| --- | --- | --- |
| `01_def_function_objects_and_aliases.py` | 用事件列表、别名和容器引用证明定义、对象身份与调用是三件事 | 只做有限的一等对象观察，不展开高阶函数 |
| `02_definition_call_and_return_timeline.py` | 把调用者、函数体、返回和调用后续放到同一时间线中 | 只解释基本形参局部绑定，不展开 C18 参数系统 |
| `03_return_paths_none_and_exceptions.py` | 对比显式返回、隐式 `None`、不可达代码和异常路径 | 不把异常误写成一种返回值，也不展开完整异常体系 |
| `04_return_values_and_side_effect_contracts.py` | 把结构化结果、显示、原地修改和输出接收者状态拆成不同合同层 | `StringIO` 只作为内存接收者，不充当持久化证明 |
| `05_docstring_signature_and_annotation_evidence.py` | 并排观察文档、签名、绑定、注解与真实调用 | 内省结果只提供有限证据，不替代运行和业务验证 |

这些脚本保持了“一个问题、一个可运行证据、一个明确不证明事项”的实验习惯。五个正式脚本已在仓库当前 `.venv-py314` 的 Python 3.14.5 下代表性运行成功；阶段测验的 Python 代码块也已通过编译验证。这种小实验比只背结论更可靠，因为它让对象身份、事件顺序、返回对象和输出接收者状态分别可观察。

本章附近还存在函数合同卡、重构验收记录和 `run_gate()` 分层等工程背景材料。它们有三项可复用的设计价值：

1. 合同卡显式记录职责、输入、返回、是否修改输入、显示或 I/O、异常、部分失败以及非职责；
2. 把结构化报告生产与 `print_report()`、`save_report()` 分开，使核心结果和边界副作用可以独立验证；
3. 使用 `if __name__ == "__main__":` 把“模块被导入”与“执行命令行入口”分开，避免导入即启动流程。

但这些材料只是当前概念的工程背景，不是 C16 的正式编号主线，也不是测验前 capstone。其部分记录来自 Python 3.13.5，不能冒充本章已完成的 3.14.5 正式验证；本章完成标准仍由启动模板、01–05 工件和阶段测验决定。

### 18.8 本阶段的理解轨迹、追问与修正规则

本章的学习表现不是从“不会写函数”起步，而是把已有对象模型提升成更精确的函数模型。几次关键作答如下。

#### 1. 同名 `def`、旧引用与元数据相等

你准确判断第二个 `def` 改变的是全局名字 `label` 的绑定，旧函数对象没有被修改，并因 `old` 的引用继续存在；也进一步注意到 `old is not label` 与 `old.__name__ == label.__name__` 可以同时为真。

应保留的最小结论是：**名字绑定、对象身份和元数据值属于不同观察层。** “名称相同”不是“对象相同”的证据。

#### 2. 共享列表修改与形参局部重新绑定

你准确区分了 `items.append("body")` 对共享列表的原地修改，以及 `items = ["local"]` 对局部名字的重新绑定；也正确判断返回 `original` 后 `result is records` 为真。

应保留的最小结论是：**函数调用建立新的局部名字绑定，但不因此复制实参对象；可见性取决于操作落在对象上还是名字上。**

#### 3. 元数据时机、编译产物与运行时函数对象

你追问了函数元数据是否等同于双下划线属性、各属性何时确定、函数体源码编译时发生什么，以及哪些错误会在编译期暴露。这不是主线缺口，而是从“`def` 不执行函数体”自然延伸出的高质量可选拓展。

应保留的修复是：不要寻找一个覆盖所有属性的单一“确定时刻”。源码解析和编译、执行 `def`、延迟求值某些注解、调用函数体是不同阶段；`__code__`、默认值和 Python 3.14 的注解机制分别跨在不同节点上。

#### 4. 异常前副作用与未完成的调用方赋值

你准确解释了异常前已执行的 `append()` 不会消失，以及 `result = risky(...)` 因右侧调用异常退出而没有完成，故 `result` 保持旧绑定。

应保留的精度修复是：异常不只是一个抽象的“跳转”；它会中断正常路径、传播并可能展开栈和执行清理，但不会自动为已经完成的副作用提供事务回滚。

本章没有出现需要重新教学的重复概念错误。P3 曾需重点防守的 `==` 与 `is`、修改与重新绑定、输出与返回、注解与强制检查等边界，在 C16 作答中总体稳定。后续仍应警惕的是**表述泛化过快**：从一个具体接收者、一次实验或一个属性观察，不要直接推出更广的接口保证。

### 18.9 阶段测验暴露的薄弱处与修正规则

阶段测验总分为 **99.75 / 100**：

| 分区 | 得分 | 判断 |
| --- | ---: | --- |
| A 概念与边界 | 16 / 16 | 稳定 |
| B 代码输出预测 | 18 / 18 | 稳定 |
| C 对象与控制流阅读 | 22 / 22 | 稳定 |
| D 易错边界 | 20 / 20 | 稳定 |
| E 工程应用 | 14 / 14 | 稳定 |
| F 综合证据判断 | 9.75 / 10 | 一处接口语义过度泛化 |

唯一实际扣分来自 F1：把 `StringIO` 的“内容和位置变化”写成任意输出接收者都具有的结果。修复方式不是再记一个特殊案例，而是始终按三层判断：

```tex
调用方发起的操作
≠ 接收者协议允许的操作
≠ 某个具体接收者实现后的可观察状态
```

此外，批改中还固定了若干不扣分但值得保留的精度修正：

1. 字符串方法返回结果且不原地修改原字符串，但不能普遍承诺结果一定具有全新的对象身份；“新字符串”应理解为规范化结果，而不是 `is not` 保证。
2. 异常中断正常路径且可能传播、展开栈并执行 `finally`；“不会自动回滚此前效果”比“异常只影响控制流”更完整。
3. `StringIO.getvalue()` 直接证明缓冲区内容；`write()` 或 `print()` 自身的返回对象，应分别由保存下来的调用结果证明。
4. 向输出对象写入不必然等于用户可见显示，更不等于持久化。
5. `404.strip()` 在查找 `strip` 属性时就失败，尚未真正调用该方法。
6. `inspect.signature()` 是当前运行时内省，注解表达设计意图，`Signature.bind()` 只提供调用形状证据；三者不能混成“类型检查通过”。
7. 对给定代码、输入和 Python 3.14.5，已观察的 `AttributeError` 是确定结论；实验覆盖有限不等于该次结果只能写成“很可能”。
8. 某实现偶然接受更多鸭子类型对象，不会自动扩大正式业务合同；本题合同仍是 `str` 及字符串列表。
9. `callable()`、签名、绑定、文档、注解或一次成功运行，任意单项都不是完整合同证明。
10. 多次写入可能留下部分效果；如果工程要求全有或全无，必须额外设计事务、临时结果或补偿策略。

这些问题共同指向一个窄而重要的提升方向：当前知识本身已经稳固，下一步不是增加术语，而是让每句话都明确**观察对象、适用环境、证明范围和不能推出的结论**。

### 18.10 工程应知应会、习惯、禁忌和技巧清单

#### 写函数前

- 用一句话写清函数的单一主要职责。
- 明确输入合同：对象应支持什么操作，业务上允许哪些值。
- 明确正常返回：返回对象的结构、各路径是否可能得到 `None`。
- 明确是否修改调用者提供的可变对象。
- 明确是否显示、写文件、访问网络或改变其他外部状态。
- 明确可能传播哪些异常，以及失败前是否可能留下部分效果。

#### 读函数时

- 先找定义名称绑定到哪个函数对象，不要看到 `def` 就假设函数体已经执行。
- 区分取函数对象、建立别名和发起调用三个动作。
- 沿每条控制流路径寻找 `return`、自然落尾和异常出口。
- 对每个赋值写清“哪个名字重新绑定到哪个对象”。
- 对每个方法调用写清“是否修改了哪个共享对象”。
- 分别记录返回值、输入修改、输出流状态和外部效果，不用一个“输出”概括全部。

#### 验证函数时

- 用 `is` 检查对象身份，用 `==` 检查值相等；不要互相替代。
- 为调用前、函数体、返回后建立最小事件时间线，定位执行顺序。
- 把调用结果保存在变量中，避免交互式显示掩盖真实返回值。
- 用 `StringIO` 验证内存输出时，同时写明它不能证明持久化。
- 用 `inspect.signature()` 和 `bind()` 检查形状时，再用真实调用验证函数体路径。
- 对成功路径、隐式 `None`、异常路径和异常前部分效果分别观察。

#### 禁忌

- 不把 `print()` 的屏幕文本称作函数返回值。
- 不把传入可变对象后的可见变化一律称作“返回了修改后的对象”。
- 不把形参局部重新绑定误写成调用方名字被改绑。
- 不把注解、文档或签名当成运行时类型强制与业务正确性证明。
- 不从一个具体输出接收者推导所有可写对象的统一刷新、位置、持久化或事务语义。
- 不因 PART opener 身份扩大到完整作用域、参数系统、高阶函数、生成器或额外 capstone。

#### 可复用技巧

- 核心函数优先返回结构化数据，显示和持久化放到边界层。
- 若函数承担副作用，让名称和文档显式表达，并记录部分失败语义。
- 用合同卡描述“职责 / 输入 / 返回 / 修改 / I/O / 异常 / 非职责”。
- 用导入保护隔离模块定义与脚本入口，避免仅导入就执行主流程。
- 用最小实验验证一个边界，并在结论旁同时写出“该实验没有证明什么”。

### 18.11 阶段精髓小结

```tex
def 的核心：创建函数对象，并绑定名字；不是立即执行函数体。
调用的核心：求值实参，建立本次调用的局部绑定，再执行函数体。
对象的核心：基本绑定不复制对象；修改对象与重新绑定名字必须分开。
退出的核心：return 正常产生调用结果；隐式 None 不是异常退出。
赋值的核心：右侧调用成功后，调用方赋值才完成。
副作用的核心：异常不会自动撤销已经完成的修改或 I/O。
合同的核心：返回、修改、显示、持久化和异常是不同观察通道。
证据的核心：文档、签名、绑定、注解与运行各有边界，任何单项都不是完整证明。
工程的核心：让数据计算与边界效果分层，并明确失败时可能留下什么。
```

C16 已把 P3 的对象与控制流基础收束成函数边界模型，也为 P4 建立了第一条有限语言主线，并已完成最终收束。C17 应在新会话中从这里的“本次调用局部绑定”继续，而不是把 C16 重讲成完整 LEGB。

---

## 19. C17 作用域：LEGB、名字解析与闭包入口

### 19.0 阶段状态和可追溯入口

- **章节角色**：`P4_Functions_and_Generators` 的 **normal** 章节；C16 是已经关闭的 PART opener，C21 才承担 PART closer，C17 既不开篇也不收束 P4。
- **唯一教学路线入口**：`docs/C17_SCOPES_STARTUP_TEMPLATE.md`。
- **正式主线工件**：
  - `01_legb_lookup_and_shadowing.py`
  - `02_local_names_and_unboundlocalerror.py`
  - `03_module_globals_and_global_statement.py`
  - `04_enclosing_names_nonlocal_and_rebinding.py`
  - `05_free_names_and_closure_entry.py`
  - `06_namespace_observation_and_explicit_dependencies.py`
- **阶段测验**：`stage_quiz_scopes.md`；A1–F1 共 `11 / 11` 题逐题审批完成，最终得分 **99.25 / 100**，结构验证与 Python 3.14.5 针对性行为验证均已通过。
- **项目关卡**：本章没有由 active-gate authority 排期的测验前 capstone；路线图中的“作用域追踪器或不依赖全局可变状态的配置函数”只是候选，不能追认为已完成关卡。
- **生命周期状态**：preparation、有限正式主线、测验生成、作答、批改、学习画像同步、本阶段末笔记、可选 follow-up 审核和 `final_closeout` 均已完成；C17 于 2026-08-12 标记为 `closed`。

本章承接 18.3 已经稳定的“每次函数调用建立自己的局部名字绑定”，但不重讲函数对象、实参求值、返回与异常时间线。C17 新增的问题是：**一个名字在哪个代码块中被分类，读取时沿哪些真实存在的词法层级查找，绑定操作最终写向哪个命名空间，以及取得对象以后发生的是重新绑定还是对象修改。**

### 19.1 有限阶段地图与名字解析本质模型

六个正式步骤组成一条有限路线：

| 步骤 | 必须回答的问题 | 稳定结论 |
| --- | --- | --- |
| LEGB 与遮蔽 | 裸名字读取会在哪里命中？ | 沿实际存在的 L、E、G、B 层寻找，第一个匹配绑定使本次查找停止 |
| 局部分类与运行期绑定 | 为什么模块层有同名对象仍会报错？ | 函数代码块先确定局部名字类别；某条运行路径尚未建立局部绑定时，读取触发 `UnboundLocalError`，不会退回 G |
| 模块全局与 `global` | 读取、修改对象和重绑模块名字有何区别？ | 读取模块名字通常无需声明；修改已找到对象不重绑名字；函数内重绑模块名字才需要 `global` |
| enclosing 与 `nonlocal` | 内层函数怎样改变外层函数绑定？ | `nonlocal` 指向词法上最近的已有外层函数绑定，不指向任意调用者或模块全局 |
| free name 与闭包入口 | 外层调用结束后为何仍能读取外层名字？ | 返回的内部函数保留访问执行所需 enclosing 绑定的能力；这不等于复制全部外层状态 |
| 命名空间观察与显式依赖 | 运行时映射能证明什么，配置应如何进入函数？ | `locals()` / `globals()` 只提供有边界的证据；影响计算的配置优先通过参数显式传入 |

贯穿全章的本质追踪链是：

```tex
先画源码中的词法嵌套
→ 判断操作是裸名字读取、简单名字绑定，还是对对象的操作
→ 确定名字类别和绑定目标
→ 沿本次真实控制流判断该绑定此刻是否已经建立
→ 读取时只走实际存在的词法层级并在首次命中后停止
→ 得到对象后，再判断是修改对象还是重新绑定名字
→ 最后记录正常结果、异常位置和异常前已完成的效果
```

必要补救不在于多背术语，而在于持续拒绝七种混写：局部命名空间不是调用方命名空间，也不是实参对象的复制；LEGB 读取和赋值目标不是同一条规则；对象可变性不决定名字类别；`UnboundLocalError` 不表示全局名字不存在；`global` / `nonlocal` 不改变对象归属；修改共享对象不自动要求作用域声明；命名空间映射也不是任意作用域的通用写回接口。

完整参数匹配留给 C18；`__closure__`、cell、符号表与字节码、闭包晚绑定的系统练习、高阶组合和复杂闭包状态留给 C19；生成器与基准分别留给 C20–C21。这些可选材料不能反向增加 C17 的完成分母。

### 19.2 名字、绑定、命名空间、作用域与名字查找

这五个概念必须分层：

| 概念 | 精确含义 | 常见误写 |
| --- | --- | --- |
| 名字 | 源码中用于引用对象的标识符 | 把名字本身说成对象或带有固定类型的盒子 |
| 绑定 | 在某个命名空间中建立或替换“名字 → 对象”的关联 | 把绑定一律说成复制对象 |
| 命名空间 | 保存一组名字绑定的运行时环境 | 把不同函数调用的局部环境说成同一个字典 |
| 作用域 | 某段源码可直接使用未限定名字访问相应命名空间的文本区域 | 把作用域说成“装对象的容器” |
| 名字查找 | 求值一个未限定名字时，确定采用哪个已有绑定的过程 | 把查找规则直接套到所有赋值和属性访问上 |

```tex
名字不是对象。
对象不被“装进”作用域。
绑定存在于命名空间中。
作用域规定源码中哪些名字绑定可以被直接解析。
一个对象可以同时被多个命名空间中的多个名字引用。
```

每次函数调用都会建立属于该次调用的局部绑定环境；同一函数的两次调用不会共享一个局部命名空间。基本调用仍然不会因此复制实参对象：两个调用中的局部名字、模块名字或 enclosing 名字完全可能引用同一个对象。

还要先识别语法类别。`obj.attr` 中只有 `obj` 是未限定名字并使用名字解析；点号后的 `attr` 由对象属性协议查找。`records.append(x)` 先解析 `records` 再调用对象方法，通常不会重新绑定名字 `records`；`records[index] = x` 也不是重新绑定基础名字。LEGB 是普通未限定名字读取的模型，不是所有 Python 语境的万能规则。

### 19.3 LEGB、遮蔽、模块全局与 builtins 边界

LEGB 四个字母表示四类可能的查找来源，而不是每段代码都机械拥有四个实体层：

| 层 | 来源 | 关键边界 |
| --- | --- | --- |
| L — Local | 当前这次函数调用的局部绑定 | 名字若已被分类为局部但此刻未绑定，不会继续向外找 |
| E — Enclosing | 源码词法上一个或多个外层函数作用域 | 可以有零层或多层；不是运行时调用者的局部环境 |
| G — Global | 当前代码所属模块的全局命名空间 | 每个模块通常各有自己的全局命名空间，不是整个程序的共享名字池 |
| B — Builtins | 内置名字所在的命名空间 | 只有更近的适用层都未命中时才成为回退层 |

遮蔽只表示本次读取在更近层先找到了同名绑定；它不会删除、修改或搬走外层绑定。查找在首次命中后停止，也不会因为所得对象“不适合下一步操作”而继续向外试另一个同名对象：

```python
import builtins

len = "module shadow"

print(len)                         # module shadow
print(builtins.len(("a", "b")))  # 2
```

裸名字 `len` 在 G 层命中字符串后就结束；`builtins.len` 则先按普通名字规则解析模块名字 `builtins`，再对模块对象执行属性查找，并不是让裸 `len` 特殊地跳过遮蔽。`globals()` 中没有 `len` 也不妨碍裸名字最终到 B 层命中，因为 builtins 并没有被合并进模块全局字典。

判断路径时必须先画真实词法结构。一个直接定义在模块层的函数没有 enclosing 函数层，其未命中的局部读取路径是 `L → G → B`，不能写成“E 存在但未命中”。函数从哪里被调用也不会把调用者局部环境插入自己的作用域链；Python 在这里采用词法作用域，而不是动态作用域。

### 19.4 局部名字分类、运行期绑定与 `UnboundLocalError`

函数局部名字分类不是逐行临时决定的。若函数代码块中存在对简单名字的绑定操作，且没有相应 `global` 或 `nonlocal` 声明，该名字会在整个函数代码块中被视为局部名字。控制流只决定某次调用是否已经执行建立该绑定的语句，不会为每条路径重新分类名字。

```python
counter = 10
events = []

def update(flag):
    events.append(f"start:{flag}")
    before = counter
    if flag:
        counter = before + 1
    return counter
```

无论 `flag` 是 `False` 还是 `True`，函数体中的简单赋值都使 `counter` 成为该函数的局部名字。执行到 `before = counter` 时，右侧尝试读取尚未绑定的局部 `counter`，因此触发 `UnboundLocalError`；它不会回退到模块层的 `counter = 10`。

完整执行轨迹应写成：

```tex
进入 update 调用
→ append 已完成，events 已留下副作用
→ 进入赋值语句 before = counter
→ 求值右侧局部 counter 时失败
→ 左侧 before 的绑定未完成
→ 后续 if、return 完全未到达
→ 异常向外传播，但此前 append 不回滚
```

“语句已经开始执行”“右侧求值完成”“左侧绑定完成”是三个不同节点。阶段测验的一个轻微扣分正来自把已经进入、但在右侧失败的赋值语句整体列为“没有执行”。

分支也只改变运行期绑定状态：

```python
def select(flag):
    if flag:
        result = ["scan"]
    return result
```

`result` 对两条路径都是局部名字；真分支先建立绑定后正常返回，假分支读取尚未绑定的 local 时触发 `UnboundLocalError`。对象是否可变与这项分类无关。

简单名字的增强赋值也同时包含读取与把结果绑定回目标，因此 `counter += 1` 仍会影响 `counter` 的局部分类。它在绑定层面不同于 `events.append(...)`：后者先取得列表对象再调用方法，通常不重新绑定名字 `events`。不能因为某个 `+=` 可能调用对象的原地协议，就忽略其简单名字目标仍承担绑定操作。

### 19.5 `global` / `nonlocal`：绑定目标与词法边界

两种声明都作用于当前代码块中的相应名字，但目标不同：

| 声明 | 绑定操作的目标 | 不会做什么 |
| --- | --- | --- |
| `global name` | 当前代码所属模块的全局命名空间 | 不把对象变成“全局对象”，不修改其它模块的同名绑定 |
| `nonlocal name` | 词法上最近的已有外层函数绑定 | 不指向模块全局，不沿运行时调用栈任意寻找调用者 |

```python
mode = "scan"
events = []

def finish():
    global mode
    events.append(mode)
    mode = "done"
```

`events.append(mode)` 读取模块名字并修改已找到的列表对象，不需要 `global events`；`mode = "done"` 要重新绑定模块名字，因而使用 `global mode`。声明改变的是名字绑定目标，不改变字符串或列表的类型、身份和可变性。

对 `global` 还要保留一个读取精度：它不会立即创建模块绑定，主要改变简单名字的绑定和删除目标。读取显式 global 名字时按全局名字规则先查当前模块 G；若模块中没有同名绑定，仍可回退到 B。因此“声明了 `global len` 且模块没有 `len`”并不必然报错，它仍可能读到内置 `len`；只有适用的 G 与 B 都没有该名字时才是 `NameError`。

`nonlocal` 则用于改变 enclosing 绑定：

```python
def outer():
    label = "outer"
    events = []

    def change():
        nonlocal label
        events.append(label)
        label = "changed"

    change()
    return label, events
```

`label = "changed"` 重绑最近的外层函数名字，所以需要 `nonlocal label`；`events.append(label)` 只是修改外层名字所指列表，不需要 `nonlocal events`。若再包一层且近、远两层都有 `label`，`nonlocal label` 只选择最近的已有 enclosing 绑定，不能任意跳到更远层。

### 19.6 跨作用域重绑、共享对象修改与闭包入口

跨作用域状态题应先写“名字—对象”账本，再判断操作：

| 操作 | 名字绑定是否改变 | 对象是否可能改变 | 是否通常需要声明 |
| --- | --- | --- | --- |
| `records.append(x)` | 否 | 是，修改已找到的列表 | 不需要，仅先读取 `records` |
| `records[key] = value` | 基础名字不变 | 是，由下标协议修改对象 | 不需要重新声明基础名字 |
| `records = []` | 是 | 创建新列表，但旧列表本身不因此改变 | 目标为 G 时用 `global`；目标为 E 时用 `nonlocal` |
| `records += [x]` | 对简单名字包含绑定回目标 | 可能先执行原地协议，也可能得到新对象 | 必须按简单名字绑定目标判断，不能等同于 `append` |

多个名字可以引用同一个对象；通过任何别名完成原地修改，其它别名通常都能观察到。重新绑定其中一个名字只改变该绑定，不会自动同步其它别名。判断共享状态时必须说清共享的是哪个对象，还是哪个 enclosing / module 绑定。

闭包入口只保留最小模型：

```python
def make_state():
    state = []

    def read_state():
        return state

    return read_state, state

reader, external = make_state()
external.append("updated")
print(reader() is external)  # True
```

对 `read_state` 而言，`state` 不是 local，也不是模块 global，而是来自 enclosing 作用域的 free name。外层调用返回后，内部函数仍能访问执行所需的外层绑定；列表没有被闭包自动复制、浅拷贝或深拷贝，所以 `reader()` 与 `external` 观察同一对象。

若再次调用 `make_state()`，该例会建立新的内部函数对象、enclosing 绑定环境和新列表；两组状态相互独立。正式主线的结论只到“free name 能继续解析、共享对象身份可追踪”为止，不检查 cell、`__closure__`。阶段笔记后的 optional follow-up 只沉淀下列与 C17 直接相连的耐久边界；它们不计入完成分母，系统的循环闭包、回调与高阶组合仍留给 C19。

#### 可选 follow-up：跨调用状态的所有者与循环闭包晚绑定

“函数跨调用保留状态”的五种常见方式不是五种作用域，而是不同的长寿命状态所有者、访问协议和共享粒度：

| 状态载体 | 真正的状态所有者 | 共享与访问边界 |
| --- | --- | --- |
| 模块全局名字 | 已加载模块的命名空间 | 使用同一模块对象的代码通常共享；`global` 只让简单名字重绑写向本模块，修改已找到对象不因此需要声明 |
| 实例属性 | 具体实例对象 | `self.attr` 使用属性协议而不是新增 LEGB 层；通常按实例隔离，是否共享仍取决于属性是否指向同一对象 |
| enclosing / 闭包 | 某次外层工厂函数调用建立的绑定环境 | 同一环境中的闭包可以共享；重绑外层名字用 `nonlocal`，再次调用工厂通常建立新环境 |
| 参数默认值 | 创建函数对象时保存的默认对象 | 省略实参时，本次调用的局部形参绑定该对象；同一函数对象共享所保存的引用，可变对象修改会累积，调用者也能显式覆盖默认值 |
| 函数属性 | 具体函数对象 | `func.attr` 使用属性协议；同一函数对象的别名共享属性，它既不是调用局部命名空间，也不是 LEGB 的新层 |

这些方式默认只保留当前进程中的内存状态，不等于持久化，也不自动提供并发安全。一个主要行为加少量私有状态时，闭包通常简洁；状态扩展到多字段、多行为、不变量或明确生命周期时，实例属性能让所有权更显式。可变默认值与函数属性不宜被当作隐蔽业务状态，模块状态也只应承载真正需要模块级共享的内容。

循环中创建多个 `lambda` 或 `def` 时，每轮通常只是重新绑定同一个循环名字；不同函数对象若都引用该 binding，延迟调用时会读取它的当前值，因而常一起看到循环最终值。这是**词法作用域下 free name 的晚绑定**，不是动态作用域；**模块顶层循环也可能呈现相同结果**，但那里延迟读取的是 global，而非 enclosing binding。

`lambda x, i=i: i ** x` 中，右侧 `i` 在创建该函数对象时求值，左侧 `i` 是以后调用中的局部形参；它固定的是当时的对象引用，不复制可变对象内容，而且该参数仍可被调用者覆盖。工厂函数可通过每次调用建立独立 enclosing 绑定，`functools.partial` 可显式固定已有函数的实参；只在原循环作用域写 `saved = i` 或加入 `nonlocal` 都不会自动产生每轮独立绑定。完整参数规则留给 C18，系统的 lambda、回调与高阶函数设计留给 C19。

### 19.7 `locals()` / `globals()` 的有限观察与显式依赖

命名空间观察必须带上调用时刻和代码块类型。对当前 Python 3.14.5 的优化函数作用域：

- `locals()` 每次返回新的字典快照；其中不仅有调用点当前已绑定的函数局部变量，也有当前可用的外层绑定引用（文档术语为 nonlocal cell references）。静态上被分类为 local 但尚未建立运行期绑定的名字，仍不会因此出现在快照中。
- 保存下来的旧 `locals()` 字典不会因之后建立新局部绑定而自动更新；修改该字典也不是可靠写回真实局部变量的接口。
- 独立新快照是 Python 3.13+ 对优化作用域的明确合同；仓库旧 CPython 3.9.13 的对照探针会在同一帧复用并刷新同一映射。某个实验若每帧只调用一次 `locals()`，两版正文输出可能相同，不能据此证明返回映射的身份或刷新语义。
- `globals()` 返回当前代码所属模块的实际全局命名空间字典；直接修改它可以影响模块绑定，但这既不是动态作用域，也不应成为普通业务状态管理接口。
- `globals()` 不合并 builtins；模块字典中没有 `len`，不妨碍普通名字读取在 G 未命中后到 B 层命中。
- 命名空间映射只能证明观察时有哪些名字绑定，不能证明对象的创建地点、唯一引用或所谓“局部/全局归属”。

因此，在 `outer → middle → inner` 的嵌套探针中，`middle` 的快照出现 `x` 不表示 `x` 由 `middle` 拥有：它是为更深层 `inner` 传递的 free reference；保留 `print(y)` 时，`y` 也是 `middle` 的 free reference，注释该读取后便不再出现。`def inner` 尚未执行时，静态 local 名字 `inner` 还没有运行期绑定，所以不会进入该次快照；`inner` 中的 `y = 20` 始终建立自己的 local。以上观察不会改变 `nonlocal x` 仍以词法方式命中最近已有外层函数绑定的规则，也不应把当前 CPython 打印字典时的键顺序升级成 `locals()` 的跨实现合同。

工程上更好的默认路线，是让真正影响计算的依赖出现在调用点：

```python
DEFAULT_LOCALE = "en-US"

def build_label(key, locale):
    return {"key": key, "label": f"{locale}:{key}"}

selected_locale = DEFAULT_LOCALE
entry = build_label("menu.start", selected_locale)
events = []
events.append(f"built:{entry['key']}")
```

核心函数只读取显式参数并返回结构化结果；调用方选择默认值并拥有事件列表，配置选择、测试输入和副作用归属都清晰可见。模块常量并非天然错误：稳定、只读约定明确的默认值可以留在模块边界；真正需要警惕的是核心计算暗中依赖可变模块状态，或通过 `globals()` / `locals()` 把数据流藏起来。

返回表达式还提醒了一个名字边界：上例字典直接写在 `return` 中，它会被创建并交给调用者，却不必先绑定给函数局部名字。作用域分类适用于名字；对象不会因为在函数体求值过程中创建就获得“局部对象”属性。正常返回后，调用方名字 `entry` 才绑定返回的字典。

### 19.8 正式工件、项目背景与工具实践的设计价值

六个正式脚本把每个机制压缩成一个可独立运行、输出确定且明确写出“不证明什么”的实验：

| 工件 | 主要设计价值 | 受控边界 |
| --- | --- | --- |
| `01_legb_lookup_and_shadowing.py` | 并排观察 L/E/G/B、首次命中、内置遮蔽与显式属性访问 | 不把 LEGB 套到所有绑定和属性协议 |
| `02_local_names_and_unboundlocalerror.py` | 分离代码块级局部分类、不同控制流路径和运行期绑定状态 | 不用对象可变性解释名字类别，不展开 C18 参数系统 |
| `03_module_globals_and_global_statement.py` | 对比模块读取、共享对象修改和模块名字重绑 | `global` 只属于当前模块，不替代跨模块架构 |
| `04_enclosing_names_nonlocal_and_rebinding.py` | 对比 enclosing 读取、局部遮蔽、共享列表修改与最近层 `nonlocal` 重绑 | 不沿动态调用栈找目标，不扩成复杂闭包状态 |
| `05_free_names_and_closure_entry.py` | 证明外层调用返回后 free name 仍可解析，且不同外层调用保留不同状态 | 不进入 cell、晚绑定和回调设计 |
| `06_namespace_observation_and_explicit_dependencies.py` | 只观察选定 namespace 键，并把隐式模块配置改成显式参数 | 不打印完整命名空间，不承诺任意映射写回 |

这些工件已在仓库 `.venv-py314` 的 Python 3.14.5 下完成 `py_compile` 和代表性运行；阶段测验又以组合题验证了输出、异常点、异常前副作用、模块最终状态、对象身份与 `locals()` 快照。编译成功证明代码块结构有效，运行探针证明给定输入和环境下的行为；二者都不应被夸大成所有输入、所有版本和全部工程合同的证明。

真实的 `prompt_template_manager` 只作为静态工程背景：模块常量可提供受控默认，调用链显式传递 `db_path` 能让依赖可见。但本章没有导入或运行该项目，没有连接 SQLite，也没有把 CRUD、CLI、GUI 或数据库配置硬包装成 `global` / `nonlocal` 教材。它不是 C17 capstone，不能替代六个自包含实验。

### 19.9 本阶段的作答轨迹、概念混淆与重复错误

本章四次主线选答已经连续证明核心模型稳定：

1. 对 `label` 与 `len` 的嵌套读取，准确写出 `label` 在 E 层命中、`len` 在 B 层命中，得到 `('enclosing', 2)`；没有把模块层后续同名重绑误写成会穿透 E 层。
2. 对分支中稍后赋值的 `counter`，准确判断 `update(False)` 与 `update(True)` 都触发 `UnboundLocalError`，并说明模块 `counter` 保持 `10`；没有把错误解释成全局名字不存在。
3. 对模块状态题，准确判断 `mode` 的重绑需要 `global`，而 `events.append(mode)` 只修改已找到列表、无需 `global events`，并得到 `True done ['scan']`。
4. 对多层嵌套题，准确判断 `nonlocal label` 命中最近的 `middle.label`，而 `events` 向上找到 `outer` 创建的列表后只做原地修改，得到 `('outer', 'changed', ['middle'])`。

因此，本章没有出现需要重新教学的重复核心错误，也没有尚待回答、会阻塞阶段笔记的概念问题。实际风险已经从“是否会用 LEGB”缩小为：写答案时偶尔把正确模型压缩得过头，省略了语句执行阶段、真实词法层级或对象是否真的绑定给名字。阶段测验的三个 `0.25` 扣分彼此独立，不能夸大成主干缺口。

### 19.10 阶段测验暴露的薄弱处与精确修复

阶段测验总分为 **99.25 / 100**：

| 分区 | 得分 | 判断 |
| --- | ---: | --- |
| A 概念与机制 | 14 / 14 | 稳定 |
| B 精确输出与遮蔽 | 18 / 18 | 稳定 |
| C 局部分类、异常与控制流 | 19.75 / 20 | 一处执行阶段表述需精修 |
| D 跨作用域状态 | 20 / 20 | 稳定 |
| E 闭包入口与命名空间观察 | 13.75 / 14 | 一处机械补出 E 层 |
| F 工程应用 | 13.75 / 14 | 一处对象创建与名字绑定混写 |

三处实际扣分及其修复如下：

| 位置 | 暴露点 | 精确修复 |
| --- | --- | --- |
| C1，`-0.25` | 把 `before = counter` 与完全未到达的后续语句一并列为“没有执行” | 该语句已进入；右侧读取局部 `counter` 时失败，左侧 `before` 绑定未完成，只有后续 `if`、第二次追加和 `return` 完全未执行 |
| E2，`-0.25` | 对模块层直接定义的函数机械写成 L → E → G → B | 先画词法树；本例没有 enclosing 函数层，实际路径是 L → G → B |
| F1，`-0.25` | 把 `return` 中直接创建的字典说成已与函数局部名字建立绑定 | 只记录真实存在的绑定；对象可直接创建并返回，之后才由调用方名字 `entry` 绑定 |

另有两项不扣分但应长期保留的精度：

1. “普通名字赋值默认建立局部绑定”必须限定在函数代码块语境；模块顶层的普通赋值建立模块全局绑定。
2. Python 3.14 优化函数的 `locals()` 是新字典快照，`globals()` 是实际模块映射；二者不能合并成任意作用域的统一写回模型。

`11 / 11` 题已经完整审批，Python 3.14.5 的针对性运行与所有客观输出、异常、副作用和身份判断一致。当前能力结论是：**中级入门前段继续稳固，C17 作用域主干达到优秀，能够用名字—作用域—对象—控制流四层模型审查小型函数状态与配置边界。** 该结论只证明 free name 与最小闭包入口，不外推为 C19 高级闭包主题已经完成。

### 19.11 工程应知应会、习惯、禁忌和技巧清单

#### 读代码时

- 先画函数的源码嵌套树，再写实际存在的 L/E/G/B 路径；不要从调用栈反推词法作用域。
- 圈出简单名字的赋值、增强赋值和声明，先确定名字类别，再按控制流判断当前是否已经绑定。
- 对每个读取记录“在哪一层首次命中”；对每个赋值记录“哪个命名空间中的哪个名字被重绑”。
- 对方法调用、属性赋值和下标赋值，先写出基础名字解析到哪个对象，再判断对象是否被修改。
- 遇到异常，分别记录已经完成的副作用、尚未完成的目标绑定和完全未到达的后续语句。

#### 设计函数时

- 让影响计算结果的配置优先通过参数显式进入核心函数，让结构化结果通过返回值离开。
- 由调用方拥有日志、事件列表和边界 I/O；若核心函数确需修改共享对象，应在合同中明确对象身份和部分失败语义。
- 模块大写常量可以表达受控默认，但大写只是约定；不要把可变业务状态藏在模块名字或 `globals()` 中。
- 只有确实要重绑模块名字或最近 enclosing 名字时才分别使用 `global` / `nonlocal`，并让共享状态范围保持小而清楚。
- 当闭包状态开始包含多项数据、复杂转换或多个写入口时，应暂停比较显式对象或类设计；具体高级闭包设计留到后续章节。

#### 验证时

- 用 `is` 验证是否仍是同一对象，用 `==` 验证值是否相等；不要从值相等推断共享身份。
- 为局部分类、运行路径和对象变化分别设观察点，不用一次最终 `print` 包办全部证据。
- 只观察 `locals()` / `globals()` 中必要的键，记录调用位置和 Python 版本；不要打印整个命名空间制造噪声或泄露无关状态。
- 对全局重绑、外层重绑和共享对象修改分别构造最小探针，并同时检查旧别名是否仍指向旧对象。
- 把 `py_compile`、代表性运行和结构检查解释为各自有限的证据，不把它们升级成完整业务正确性证明。

#### 禁忌与可复用技巧

- 不说“局部对象”“全局对象”；应说哪个命名空间中的名字绑定到哪个对象。
- 不把 `UnboundLocalError` 简化为模块或 builtins 中没有同名名字。
- 不机械写满 LEGB 四层，也不把运行时调用者局部环境当成 E。
- 不把 `global` / `nonlocal` 当成通用报错补丁；使用声明等于选择了相应共享状态设计。
- 不用对象可变性决定名字作用域，也不把 `append()` 与简单名字 `+=` 的绑定语义混成一类。
- 不用 `locals()` / `globals()` 写回代替正常参数、返回值和状态对象。
- 可复用的审查表是：**词法树 / 名字类别 / 绑定目标 / 当前绑定状态 / 命中层 / 对象身份 / 修改或重绑 / 异常前效果**。

### 19.12 阶段精髓小结

```tex
名字的核心：名字只是标识符；对象没有由作用域赋予的“局部/全局”属性。
绑定的核心：绑定是某个命名空间中的“名字 → 对象”关系，不自动复制对象。
作用域的核心：作用域是源码中的词法可见区域；命名空间是运行时绑定环境。
读取的核心：裸名字沿实际存在的词法层级查找，并在第一个匹配绑定处停止。
局部的核心：函数代码块先分类名字；运行路径再决定该局部绑定此刻是否已建立。
报错的核心：读取尚未绑定的 local 触发 UnboundLocalError，不会退回模块同名绑定。
声明的核心：global 指向当前模块绑定，nonlocal 指向最近外层函数绑定；二者不改变对象。
状态的核心：修改已找到对象与重新绑定名字是两条路径，声明需求取决于绑定目标而非可变性。
闭包的核心：内部函数可在外层调用结束后继续访问所需 enclosing 绑定，不自动复制共享对象。
观察的核心：locals() / globals() 只能提供带版本、代码块和时点边界的命名空间证据。
工程的核心：让依赖通过参数进入，让结果通过返回值离开，让副作用所有权留在清晰边界。
证据的核心：每个结论都要同时说明命中层、对象身份、控制流阶段和不能推出的范围。
```

C17 已把 C16 的“本次调用局部绑定”扩展成完整而有限的名字解析模型，并完成阶段末笔记与最终收束，生命周期为 `closed`。下一章唯一入口是 `docs/C18_ARGUMENTS_STARTUP_TEMPLATE.md`；应另开新会话执行 C18 preparation，不在本节提前开始参数主线。

---

## 20. C18 参数：实参求值、形参绑定与接口合同

### 20.0 阶段状态和可追溯入口

- **章节角色**：`P4_Functions_and_Generators` 中的普通章节（`normal`），不是 PART opener 或 closer。
- **唯一教学路线入口**：`docs/C18_ARGUMENTS_STARTUP_TEMPLATE.md`。本节只收束该模板冻结的 C18 范围；同主题脚本只提供运行证据，不反向增加必学内容。
- **正式编号学习工件**：`01_argument_evaluation_matching_and_local_binding.py` 至 `06_positional_keyword_only_and_signature_evidence.py` 六个实验在 preparation 阶段落地，随后作为主线运行锚点，并已在当前 Python 3.14.5 学习环境中完成语法检查和代表性运行。
- **阶段测验**：`stage_quiz_arguments.md` 已逐题审批 11/11 题，稳定总分为 **99.25 / 100**；扣分点已经转化为本节的精度修复。
- **学习画像**：`notes/Python_Learning_Profile.md` 中的 C18 末评证据表明，参数调用合同主线已达到优秀水平；当前风险主要是阶段语言、引用图方向和函数体内重绑定的表达精度，而不是机制性缺口。
- **capstone 关卡**：active-gate authority 没有为 C18 排期测验前 capstone；路线图中的项目候选不算已排期项目，因此本章没有需要补做或冒充完成的 capstone。
- **当前生命周期**：`mainline`、`quiz_authoring`、`quiz_answering` 与 `quiz_review` 均已完成，阶段末笔记在本节完成；下一关是 `final_closeout`。此处不提前把 C18 标为 `closed`，也不创建或启动 C19。

本章承接 C16 已建立的“实参表达式产生对象、形参成为本次调用的局部名字、绑定不复制对象”，但把它扩展成一套完整而有限的调用合同。C17 的 LEGB、闭包和状态所有者只作为既有基础，不在这里重复。

### 20.1 有限阶段地图与参数调用本质模型

#### 本章有限路线

| 步骤 | 必学问题 | 主要证据 |
| --- | --- | --- |
| 1 | 调用目标、实参表达式、输入装配、签名匹配、局部绑定和函数体按什么顺序发生？ | `01` 的事件时间线 |
| 2 | 五类 parameter kind 如何接收输入，哪些调用形状会失败？ | `02` 的绑定矩阵与失败探针 |
| 3 | 默认表达式何时求值、对象保存在哪里、为何可变默认值会跨调用共享？ | `03` 的定义事件、身份与安全对照 |
| 4 | 定义端 `*args` / `**kwargs` 收集什么，形成的外层收集容器是否复制内部对象？ | `04` 的 tuple、dict 与嵌套别名 |
| 5 | 调用端 `*` / `**` 如何展开输入，冲突在哪一阶段暴露，失败前效果是否回滚？ | `05` 的解包来源和事件记录 |
| 6 | `/`、keyword-only 与签名内省如何服务 API；`signature` / `bind` 能证明什么？ | `06` 的证据阶梯 |

#### 必学核心、必要补救和可选边界

| 层级 | 当前结论 |
| --- | --- |
| **必学核心** | 上表六步，以及参数缺失、重复、未知、过量和 positional-only 误用等稳定失败边界 |
| **必要补救** | 时间阶段不能压成“同时”；引用箭头必须是“名字或槽位 → 对象”；`None` 初始绑定与函数体内新字典重绑定必须分开 |
| **可选拓展** | 当 `None` 是合法业务值时可用专用 sentinel；复杂多组星号展开、`bind_partial()` 和更深的 inspect 内部机制不进入本章必学主线 |

#### 两条时间线不能混为一条

执行函数定义时：

```text
执行普通 def（不展开装饰器）
  → 求值默认表达式
  → 用代码、参数信息和默认对象创建并配置函数对象
  → 在当前命名空间绑定函数名
```

执行调用表达式时：

```text
求值调用目标
  → 按实际顺序求值普通实参及 * / ** 来源
  → 展开并装配位置输入和关键字输入
  → 按签名检查唯一匹配并补用默认对象
  → 成功建立本次调用的完整初始形参绑定
  → 进入函数体
  → 才有函数体异常、返回值和函数体副作用
```

因此，本章最稳定的主模型不是“按值传递”或“按引用传递”这两个容易失真的单句标签，而是：

> 实参表达式先得到对象；调用端把对象装配为位置和关键字输入；签名决定输入能否唯一映射到形参；成功后，形参成为本次调用的局部名字并绑定这些对象。绑定本身不复制对象。

同时保留三个观察维度：

- **名字维度**：哪个名字或容器槽位绑定、保存了哪个对象。
- **对象维度**：是同一对象被原地修改，还是某个名字被重新绑定。
- **控制流维度**：求值、装配、匹配、初始绑定和函数体究竟完成到哪一步。

### 20.2 调用时间线：求值、装配、匹配、绑定与函数体

#### 六个概念必须分开

| 概念 | 精确含义 |
| --- | --- |
| 形参（parameter） | 函数定义中的名字及其 kind/default 合同 |
| 实参（argument） | 调用时提供给函数的输入 |
| 实参表达式 | 调用源码中产生实参对象的表达式 |
| 输入装配 | 把普通实参和 `*` / `**` 来源形成位置输入、关键字输入 |
| 参数匹配 | 按签名判断每个输入是否能唯一对应形参或收集器 |
| 初始局部绑定 | 成功调用后，本次调用的形参局部名字到对象的完整映射 |

最小事件探针：

```python
events = []


def mark(label, value):
    events.append(label)
    return value


def fail():
    events.append("argument:locale")
    raise ValueError("bad locale")


def build_entry(key, locale, options):
    events.append("body")


try:
    mark("target", build_entry)(
        mark("argument:key", "menu.start"),
        fail(),
        mark("argument:options", {}),
    )
except ValueError:
    pass

print(events)
```

稳定输出为：

```text
['target', 'argument:key', 'argument:locale']
```

这份证据只允许推出：

1. 调用目标、`key` 实参和 `locale` 实参已依次求值；
2. `locale` 求值抛错后，后面的 `options` 根本没轮到求值；
3. 外层调用没有完成输入装配与签名匹配，也没有建立可供函数体使用的完整形参绑定；
4. `build_entry` 函数体没有执行，所以没有 `"body"` 事件。

不要把第 3 点扩大成对解释器内部是否短暂创建某种帧对象的实现断言。教学和工程上需要的是可观察合同：**没有成功调用的完整参数绑定，目标函数体没有进入**。

#### 失败点决定能保留哪些结论

| 失败点 | 已可能发生 | 尚不能声称 |
| --- | --- | --- |
| 调用目标求值失败 | 目标表达式此前的效果 | 任一实参求值、匹配、函数体 |
| 某个实参或解包来源求值失败 | 更早表达式的效果 | 后续输入、完整装配、匹配、函数体 |
| 调用输入装配失败 | 已求值来源的效果 | 成功匹配、完整形参绑定、函数体 |
| 签名匹配失败 | 已完成的求值与装配效果 | 成功调用的局部绑定、函数体 |
| 函数体失败 | 完整初始形参绑定及此前函数体效果 | 正常返回和后续语句 |

异常不会自动回滚此前已经完成的列表追加、日志记录或外部调用。分析失败调用时，必须同时记录“已经发生”和“尚未发生”。

### 20.3 五类形参与绑定失败

以下签名集中展示五种 parameter kind：

```python
def bind_matrix(
    record_id, /,
    locale="en-US",
    *tags,
    dry_run,
    **metadata,
):
    return record_id, locale, tags, dry_run, metadata
```

| kind | 示例 | 可以怎样获得输入 | 省略时的边界 |
| --- | --- | --- | --- |
| positional-only | `record_id` | 只能按位置 | 无默认值则缺失 |
| positional-or-keyword | `locale` | 按位置或关键字二选一 | 这里可用默认对象 |
| var-positional | `*tags` | 收集剩余位置输入为 tuple | 没有剩余输入时为空 tuple |
| keyword-only | `dry_run` | 只能按关键字 | 这里无默认值，必须提供 |
| var-keyword | `**metadata` | 收集剩余关键字输入为新 dict | 没有剩余输入时为空 dict |

`/` 和裸 `*` 都只是签名分隔符，不创建局部名字；`*tags` 与 `**metadata` 中的 `tags`、`metadata` 才是实际形参名。

例如：

```python
result = bind_matrix(
    "prompt-002",
    "fr-FR",
    "dialog",
    dry_run=False,
    reviewer="qa",
)
```

得到的绑定是：

```text
record_id = 'prompt-002'
locale = 'fr-FR'
tags = ('dialog',)
dry_run = False
metadata = {'reviewer': 'qa'}
```

额外位置值会先进入 `*tags`，不会越过它去绑定 `dry_run`；由于这里 `dry_run` 没有默认值，若调用也没有写 `dry_run=...`，最终会因缺少必需 keyword-only 参数而失败。

#### positional-only 的精细边界

```python
def keep_alias(record_id, /, **metadata):
    return record_id, metadata


print(keep_alias("prompt-004", record_id="external-alias"))
```

这里位置输入绑定真正的 `record_id` 形参；同名关键字不允许绑定 positional-only 形参，但可以作为未消费的普通关键字进入 `metadata`。结果是：

```text
('prompt-004', {'record_id': 'external-alias'})
```

如果函数没有 `**metadata`，仅写 `record_id="prompt-004"` 就会失败。不要把“名字相同”误当成“关键字有权绑定该形参”。

#### 常见失败矩阵

| 调用形状 | 稳定判断 |
| --- | --- |
| 必需形参没有输入且无默认值 | 缺失参数，`TypeError` |
| 同一形参既收到位置输入又收到同名关键字 | 重复提供，`TypeError` |
| 未知关键字且没有 `**kwargs` | 未知关键字，`TypeError` |
| 位置输入过多且没有 `*args` | 位置输入过量，`TypeError` |
| positional-only 只按同名关键字提供 | 不能绑定该形参，调用失败 |

完整 `TypeError` 文本可能随 Python 版本和 callable 类型变化。耐久笔记应记录异常类型、调用形状、失败阶段和函数体是否执行，不把整段错误消息当成跨版本合同。

### 20.4 默认值：定义时保存、调用时复用与可变对象风险

#### 默认值回答的是对象来源，不是另一种作用域

```text
执行一次 def
  → 默认表达式求值一次
  → 函数对象保存所得对象

某次调用省略该实参
  → 用保存的默认对象补该形参
  → 形参仍是本次调用的局部名字
```

每次真正再次执行同一个 `def`，都会为那次函数对象创建过程重新求值默认表达式；但对已经创建好的同一函数对象，省略实参不会重跑默认表达式。`function.__defaults__` 和 `function.__kwdefaults__` 可以提供保存对象的有限观察证据，通常不应把直接改写它们当成业务设计手段。

#### 可变默认值为何危险

```python
def remember(channel, history=[]):
    history.append(channel)
    return history


first = remember("menu")
second = remember("dialog", [])
third = remember("settings")

print(first)
print(second)
print(third)
print(first is third)
```

输出是：

```text
['menu', 'settings']
['dialog']
['menu', 'settings']
True
```

原因不是“列表形参特殊”，也不是 `global` / `nonlocal`：

1. 默认列表在执行 `def` 时只创建一次；
2. 第一次和第三次省略实参，形参都绑定到同一默认列表；
3. `append()` 原地修改该对象；
4. `first`、`third` 和函数保存的默认槽位都能观察到同一对象；
5. 第二次显式传入新列表，使用的是另一个对象；
6. 四行打印发生在三次调用之后，所以 `first` 已能看到第三次修改。

#### 每次调用需要独立状态时

```python
def remember(channel, history=None):
    if history is None:
        history = []
    history.append(channel)
    return history
```

这里必须按两个阶段描述：

```text
调用入口：history → None
函数体内：创建新列表
随后重绑：history → 新列表
```

不能把它压缩成“参数一开始就绑定新列表”。这个模式只保证省略实参或显式传入 `None` 时创建新对象；若调用者显式传入列表，函数仍会按合同修改那个共享列表。是否复制显式传入的对象，是另一个所有权决定。

“`None` 表示省略”是 API 语义约定，代码判断应写 `is None`，不是做 `==` 等值推理。如果 `None` 本身是合法业务值，才需要考虑专用 sentinel；本章只保留这条边界，不展开 sentinel 专题。

### 20.5 定义端收集：收集容器与元素对象的浅层别名

```python
def plan_record(primary_key, *extra_keys, **controls):
    return primary_key, extra_keys, controls


channels = ["menu"]
primary_key, extra_keys, controls = plan_record(
    "menu.start",
    "menu.quit",
    channels,
    locale="ja-JP",
)

channels.append("dialog")
print(extra_keys)
print(controls)
print(extra_keys[1] is channels)
```

稳定输出为：

```text
('menu.quit', ['menu', 'dialog'])
{'locale': 'ja-JP'}
True
```

这里同时存在两类事实：

- `extra_keys` 是本次调用为 var-positional 形参形成的 tuple；
- `controls` 是本次调用为 var-keyword 形参形成的新 dict；
- 形成外层收集容器不等于深拷贝元素；
- `extra_keys[1]` 与调用方 `channels` 仍指向同一列表，所以原地修改能从两处观察到。

没有剩余位置或关键字输入时，收集结果分别是空 tuple 和空 dict。不要依赖空 tuple 是否复用某个具体身份；语言合同在这里需要的是类型、内容和内部引用关系，而不是实现级缓存身份。

`*args` 不是“所有实参”，`**kwargs` 也不是“所有关键字实参”：

```text
*args     = 被前面形参匹配后剩余的位置输入
**kwargs  = 被其他形参匹配后剩余的关键字输入
```

开放 `**kwargs` 会扩大调用形状，却不会自动提高调用正确性。普通业务函数若本可显式声明字段，却用 `**kwargs` 吞下所有未知键，拼写错误就可能从清晰的调用错误拖延成更晚的业务错误。

### 20.6 调用端解包：输入来源、重复冲突与失败前效果

定义端和调用端的星号承担不同职责：

| 位置 | 形式 | 作用 |
| --- | --- | --- |
| 定义端 | `*args` | 把剩余位置输入收集为 tuple 并绑定局部名字 |
| 定义端 | `**kwargs` | 把剩余关键字输入收集为 dict 并绑定局部名字 |
| 调用端 | `*iterable` | 展开 iterable 的元素，形成多个位置输入 |
| 调用端 | `**mapping` | 展开 mapping 的键值对，形成多个关键字输入 |

调用端 `*` 来源必须可迭代；`**` 来源必须是合适的 mapping，且用作关键字的键必须是字符串。展开只形成调用输入，随后仍须服从目标函数签名；它不会让目标函数凭空拥有 `*args` / `**kwargs`，也不会绕过 positional-only、keyword-only 或重复匹配检查。

下面的事件探针精确区分来源求值和调用失败：

```python
events = []


def source(label, value):
    events.append(label)
    return value


def target(*, dry_run):
    events.append("body")


try:
    target(
        *source("positional", ()),
        dry_run=source("explicit", True),
        **source("mapping", {"dry_run": False}),
    )
except TypeError:
    events.append("TypeError")

print(events)
```

输出为：

```text
['positional', 'explicit', 'mapping', 'TypeError']
```

三个来源都已求值；显式 `dry_run=True` 与 mapping 中的同名键在装配关键字输入时冲突，因此目标函数体没有执行，也没有 `"body"` 事件。不能说后来的 mapping 值“覆盖”先前的显式关键字。

还要区分两类重复：

- 显式关键字与 `**mapping` 提供同名键，可在关键字输入装配时失败；
- 位置输入与关键字输入最终都要填同一个 positional-or-keyword 形参，会在签名匹配时形成重复提供。

对工程结论而言，两者的共同稳定边界是：调用失败、目标函数体不执行、此前求值效果不回滚；对教学分析则应尽量保留更精确的失败阶段。

一个容易误迁移的对照是：

```python
merged = {**{"locale": "en-US"}, **{"locale": "ja-JP"}}
```

字典构造允许后一个键值覆盖前一个；**函数调用中的重复关键字不采用这条覆盖规则，而是失败**。语法外形相似，不代表合同相同。

### 20.7 inspect.signature() 与 Signature.bind() 的证据边界

#### 证据强度阶梯

| 层级 | 能增加的证据 | 仍不能证明 |
| --- | --- | --- |
| 阅读源码签名 | 声明出的 kind、default 和开放收集边界 | 实际运行、业务结果 |
| `inspect.signature()` | callable 当前对外呈现的可观察签名 | 函数体正确、所有 callable 都一定可内省 |
| `Signature.bind()` | 已交给它的位置/关键字对象能按这份签名唯一映射 | 目标函数体、类型可用性、返回值、副作用和业务规则 |
| 实际调用 | 这组具体输入下的函数体路径、异常、结果和副作用 | 其他输入与外部状态也正确 |
| 多案例业务验证 | 已覆盖案例中的合同证据 | 未覆盖空间自动正确 |

```python
import inspect


def render_record(record_id, /, text, *, locale="en-US"):
    return text.upper(), record_id, locale


signature = inspect.signature(render_record)
bound = signature.bind("prompt-008", "quit", locale="ja-JP")

print(signature)
print(bound.arguments)
```

`bind()` 成功只说明这组已经提供给 `bind()` 的对象适配当前观察到的 `Signature`。它：

- 不执行 `render_record`；
- 不证明另一个原始调用表达式中的实参求值或 `*` / `**` 来源展开能够成功；
- 不证明 `text` 一定支持 `upper()`；
- 不证明返回结果满足本地化业务合同；
- 不证明没有函数体副作用或外部资源失败。

需要特别保留的精度是：`bind()` 面对的是已经交给它的位置和关键字对象，不是对另一段原始调用源码的全流程预演。

`inspect.signature()` 得到的是 callable **呈现出来的签名**。某些 callable 可能无法取得签名，可定制的 `__signature__` 也可能使呈现签名与实际调用行为产生偏差。因此，`signature()` / `bind()` 是很强的调用形状证据，却不能取代真实调用和业务测试；也不应在 C18 顺势扩张成完整 inspect 内部机制专题。

### 20.8 工程接口设计：稳定调用形状、显式配置与防误用

参数语法最终服务于 API 合同，而不是为了展示所有符号。设计普通业务函数时依次问：

| 问题 | 可考虑的合同 |
| --- | --- |
| 这个输入的位置本身是否自然、稳定，名字不是公共合同的重要部分？ | positional 或 positional-only |
| 参数名是否显著提高可读性，尤其是布尔开关和同类型配置？ | keyword-only |
| 调用者省略时是否确有稳定合理的备用对象？ | default |
| 输入数量是否真正开放且同质？ | `*args` |
| 未知关键字是否真是正式扩展点或转发合同？ | `**kwargs` |
| 每次省略都需要独立可变状态吗？ | 函数体内创建，不用可变默认对象 |

本地化记录接口可以表达成：

```python
def get_record(record_id, /):
    ...


def create_record(*, key, text, locale="en-US"):
    ...


def update_record(record_id, /, *, text=None, locale=None, dry_run=False):
    ...
```

这些签名分别强调：

- 稳定身份值可以只占位置，避免把参数名意外固化为调用方合同；
- 创建记录时，字段名本身有解释价值，全 keyword-only 能提高审查可读性；
- 更新记录把身份值与可选更新项分离，`dry_run=False` 必须显式命名，避免 `True` / `False` 的位置含义含糊；
- `text=None`、`locale=None` 是否表示“不更新”必须由业务合同明确；如果 `None` 是合法更新值，就不能继续沿用这一约定。

#### 工程习惯

- 固定核心输入优先显式声明；不要仅为“以后也许扩展”添加 `**kwargs`。
- 布尔控制项和多个同类型配置优先 keyword-only。
- 同时使用 `*tags` 与 keyword-only 控制项时，要明确额外位置输入都会成为 tag；这是合同取舍，不是自动防止所有误传。
- 函数若会修改调用者传入的列表或字典，应声明所有权；必要时按合同浅拷贝，但浅拷贝仍不隔离嵌套可变对象。
- 失败前实参求值可能已有外部效果，因此有副作用的实参工厂应保持清楚、可观察且尽量小。
- 用 `Signature.bind()` 检查调用形状；用真实调用验证函数体；用多案例验证业务规则。三者不能互相替代。

#### 工程禁忌

- 不用“Python 把变量复制进函数”解释参数绑定。
- 不把可变默认值当成省事的跨调用缓存；它没有显式键、边界、失效策略或并发合同。
- 不把 `**kwargs` 当作拼写错误保险箱。
- 不依赖完整 `TypeError` 文本或空容器身份等实现细节。
- 不从一次 `bind()` 成功推出类型正确、函数体无异常或业务结果正确。
- 不把项目中某个真实签名反向升级为整章必学范围。

### 20.9 正式工件、项目背景与工具实践的设计价值

| 工件 | 设计价值 |
| --- | --- |
| `01_argument_evaluation_matching_and_local_binding.py` | 用事件列表显露目标求值、实参求值、函数体进入和失败前效果；用 `is` 证明绑定不复制字典 |
| `02_parameter_kinds_and_binding_failures.py` | 用一个签名覆盖五类形参，再以严格函数和 body-event 探针验证缺失、重复、未知、过量及 positional-only 失败 |
| `03_defaults_and_mutable_default_state.py` | 把定义事件、默认对象身份、危险版与 `None` 安全版并列，证明默认对象的创建、保存和复用 |
| `04_varargs_and_varkw_collection.py` | 同时观察 tuple、dict 外层收集容器和嵌套列表别名，阻止“收集等于深拷贝”的误解 |
| `05_call_unpacking_conflicts_and_partial_effects.py` | 用来源事件串起调用端解包、重复输入和失败前副作用，区分 collection 与 unpacking |
| `06_positional_keyword_only_and_signature_evidence.py` | 并列呈现签名、`bind()`、实际调用与函数体异常，形成可复用的证据强度阶梯 |
| `stage_quiz_arguments.md` | 用概念解释、代码预测、对象图、控制流和工程 API 题检验迁移能力，并留下逐题批改记录 |

这些工件的共同价值不是“打印出正确答案”，而是让每条结论对应一个最小观察点：

```text
事件列表  →  时间与部分失败
is         →  对象身份
已知函数原样回传全部形参
          →  观察该案例的绑定结果
异常类别 + 调用形状/事件轨迹
          →  定位稳定失败边界
signature →  呈现出的调用形状
bind      →  对该签名的映射证据
真实调用  →  函数体、结果与副作用
```

`projects/P3_Statements_and_Syntax/prompt_template_manager/` 只作为只读工程背景：它说明 keyword-only 控制项、显式配置和稳定调用合同为何有价值。它没有覆盖 C18 全部参数机制，也不是本章 active-gate 排期的 capstone；SQLite、CRUD、CLI、GUI 等项目细节不进入本章笔记范围。

### 20.10 本阶段的作答轨迹、概念混淆与修复

本章的选答题不是进度门槛，但提供了高质量定位证据：

1. **实参求值中断**：准确写出 `['target', 'argument:key', 'argument:locale']`，并指出 `options` 未求值、匹配和函数体均未发生。
2. **五类参数绑定**：准确还原 `record_id`、`locale`、`tags`、`dry_run`、`metadata`，并说明 `/` 不创建局部名字、额外位置值由 `*tags` 收集。
3. **positional-only 同名关键字**：准确识别真正形参由位置输入绑定，而同名关键字在存在 `**metadata` 时可以作为普通元数据键被收集；没有收集器时调用失败。
4. **可变默认值**：准确预测跨调用共享列表、显式新列表隔离和 `first is third`，并注意打印发生在所有调用之后。
5. **收集容器的浅层边界**：准确区分新 tuple 与其中共享列表，能用原地修改和 `is` 解释别名可见性。
6. **解包冲突**：准确记录位置来源、显式关键字、mapping 来源都先产生事件，随后 `TypeError`；目标函数体不执行。

这组表现说明没有持续重复的核心机制错误。真正需要修复的是三种“高压压缩偏差”：

- **阶段压缩**：容易把不同时间点的别名关系写成“同时”，需要按求值、装配、匹配、局部绑定、返回后调用方绑定逐节点陈述。
- **术语压缩**：重复关键字可进一步区分调用输入装配冲突与对同一形参的匹配冲突；若只需稳定工程结论，则至少明确“调用失败且函数体不执行”。
- **图示压缩**：箭头必须统一表示“名字或容器槽位 → 对象”，不能让对象看起来反向拥有 tuple/dict 槽位。

另一个应长期保持的边界是：回答“没有进入函数体、没有成功的完整形参绑定”已经足够，不需要把结论扩大成未经观察的解释器帧实现。拓展回答展现了迁移能力，但没有因此扩张本章必学范围。

### 20.11 阶段测验暴露的薄弱处与精确修复

#### 稳定成绩

| 分区 | 得分 |
| --- | ---: |
| A 概念解释与调用时间线 | 13.75 / 14 |
| B 参数种类、绑定矩阵与失败边界 | 18 / 18 |
| C 默认值、对象身份与可变状态 | 18 / 18 |
| D 定义端收集、调用端解包与部分效果 | 19.75 / 20 |
| E 签名观察、绑定记录与证据强度 | 14 / 14 |
| F 本地化记录 API 工程设计 | 15.75 / 16 |
| **总分** | **99.25 / 100** |

逐题覆盖为 11/11。结果证明已经能把参数规则迁移到新的调用、对象图和工程签名，而不是只记住正式脚本输出。

#### 三处扣分及修复

| 题目 | 暴露的问题 | 精确修复 |
| --- | --- | --- |
| A1（-0.25） | 把函数体中的局部别名与函数返回后调用方结果名字写成“同时”指向对象 | 分开写：函数体运行时是局部形参/局部名字；函数返回并完成调用方赋值后，才新增调用方结果名字 |
| D1（-0.25） | 对象图箭头可能被读成“列表反向指向 tuple/dict 槽位” | 固定箭头语义为“名字或容器槽位 → 对象”，共享对象只画一个节点，多条引用箭头指向它 |
| F1（-0.25） | 把省略 `metadata` 后的新字典直接列为“参数键绑定” | 调用入口先是形参 `metadata → None`；进入函数体执行 `metadata = {}` 后，局部名字才重绑到新字典 |

#### 未扣分但必须保留的精度边界

- “没有 frame”应收窄成“没有目标函数体可观察到的成功完整绑定，函数体未进入”，避免把课程结论建立在内部实现上。
- 默认工厂若向模块事件列表追加内容，那是模块列表的独立副作用；跨调用共享默认对象则来自函数对象保存默认值，两种机制不能混称。
- “`None` 表示省略”是业务合同，判断写 `is None`。
- `inspect.signature()` 是呈现签名，可能被定制或过时；`bind()` 成功不证明实际 callable 的函数体合同。
- 浅拷贝只新建外层容器，不能据此称函数已经深度隔离或成为严格纯函数。

末评中的薄弱点都属于表达精度而非核心理解失败。修复策略不是增加更多高级主题，而是强制每个答案带上四个标签：

```text
时间阶段 / 引用持有者 / 对象身份 / 证据上限
```

### 20.12 工程应知应会、习惯、禁忌和技巧清单

#### 读调用时

- 先读签名，为每个形参标出 kind 和 default，再处理调用端输入。
- 记录调用目标与各输入来源的实际求值顺序；必要时用事件探针验证，不把书写位置直接等同于求值顺序或最终绑定顺序。
- 先形成位置/关键字输入，再检查重复、未知、过量和缺失；不要从函数体倒推匹配一定成功。
- 对失败调用分别写“已完成效果”和“未发生阶段”，不假设异常会回滚。
- 对每条对象关系使用“名字或槽位 → 对象”，再判断是原地修改还是名字重绑。

#### 写接口时

- 固定输入优先显式声明；名称有解释价值的配置优先 keyword-only。
- 只有真实开放输入或转发职责才使用 `*args` / `**kwargs`，并记录允许范围。
- 每次调用需要独立可变状态时，在函数体内创建；不要把 mutable default 当成隐式缓存。
- 明确函数是否修改调用者传入对象；需要复制时说明浅拷贝还是更深的隔离。
- 对 `None`、空字符串、空容器和省略实参分别写清业务语义，不让默认值替业务验证。

#### 验证时

- 用事件列表验证时间顺序，用 `is` 验证身份，用结构化返回值验证绑定结果。
- 对预期失败记录异常类别和 body-event，不依赖跨版本完整错误文本。
- 用 `signature()` 观察呈现合同，用 `bind()` 验证调用形状，用实际调用观察行为。
- 至少构造正常、缺失、重复、未知、过量、positional-only 误用和函数体失败等最小对照。
- 让证据强度匹配结论强度：一次运行只证明该环境下该组输入的行为。

#### 禁忌与可复用技巧

- 不用“按值/按引用”单句替代名字和对象模型。
- 不把形参绑定说成复制对象，也不把调用方名字和函数局部形参混成同一绑定。
- 不混淆定义端收集与调用端展开。
- 不说“后一个关键字覆盖前一个”；函数调用发生同名关键字冲突时应按失败处理。
- 不把 `/`、裸 `*` 当成局部名字，也不让额外位置输入越过 `*args` 去绑定 keyword-only 参数。
- 可复用审计表是：**目标求值 / 输入来源 / 装配结果 / parameter kind / default / 唯一匹配 / 初始局部绑定 / 函数体 / 返回或异常 / 既有副作用**。

### 20.13 阶段精髓小结

```tex
定义的核心：执行普通 def 时先求值默认表达式，再创建并配置函数对象、建立参数合同和保存默认对象。
求值的核心：调用目标和实参来源先产生对象；较早异常会阻止后续输入继续求值。
装配的核心：调用端 * / ** 先形成位置和关键字输入，不直接绑定目标函数形参。
匹配的核心：Parameter.kind 决定输入渠道；每个必需形参最终必须得到唯一输入。
绑定的核心：成功调用让本次调用的局部形参名字绑定对象，绑定本身不复制对象。
默认的核心：省略实参会复用函数对象保存的默认对象；可变对象被原地修改便会跨调用显现状态。
收集的核心：*args 绑定 tuple，**kwargs 绑定新 dict；两种外层收集都不会深拷贝其中对象。
失败的核心：缺失、重复、未知、过量或错误 kind 会阻止函数体，却不回滚此前求值效果。
接口的核心：positional-only 控制名称承诺，keyword-only 强化可读性，开放收集必须有真实合同。
证据的核心：signature 呈现形状，bind 验证映射，实际调用观察行为，业务案例检验结果。
工程的核心：显式输入、清楚所有权、稳定失败边界与匹配结论强度的证据，比宽泛签名更可靠。
```

C18 已完成正式主线、阶段测验审批和阶段末笔记；本章角色仍是 `normal`，且没有已排期的测验前 capstone。当前生命周期停在 `stage_note` 完成后的 `final_closeout` 入口，尚未标记 `closed`。最终收束应另按 closeout ledger 核验本章耐久记录和下一章身份，不在本节提前启动 C19。
