# C11 Assignments, Expressions, and Prints Startup Template

下面的内容用于新开会话，正式进入
`P3_Statements_and_Syntax / C11_Assignments_Expressions_and_Prints`。它不是
C10 的继续讲解，也不是一次性展开整个 P3。新会话应只正式推进 C11；C12 的
条件判断、真值测试和 `match` 只作为必要预告出现。

```text
<Subject>
当前新开会话的启动模板（可复用）：赋值、表达式语句与输出：名字绑定、原地修改和 print 边界
</Subject>

<Contents>
【阶段名称】
当前大阶段：学习 Python 的语句和语法（Statements and Syntax）

已完成的大阶段：
1. `P1_Getting_Started`（已完成）
2. `P2_Types_and_Operations`（已完成并通过收束验收）

已完成的小阶段：
1. `C10_Introducing_Python_Statements`（已完成，阶段小测建议得分 `96 / 100`）

当前小阶段：`C11_Assignments_Expressions_and_Prints`

主要内容包括但不限于：
赋值语句的本质；名字绑定；赋值目标；多目标赋值；链式赋值；序列解包；
星号解包；属性赋值；下标赋值；切片赋值；增强赋值；原地修改与重新绑定；
注解赋值的预告；表达式语句；裸表达式在脚本与 REPL 中的差异；`print()`
作为函数调用表达式；`print()` 的输出副作用和返回值 `None`；`sep`、`end`、
`file`、`flush` 参数；输出流、日志和文件写入的工程边界。

本小阶段要把 C10 建立的“表达式求值 vs 语句执行”模型压到赋值和输出细节中。
后续 C12 的条件判断、C13 的循环和 C14 的迭代只做必要预告，不提前系统展开。

---

【我当前的位置】
✔ 已掌握：
- 已完成 `C10_Introducing_Python_Statements`，阶段小测建议得分 `96 / 100`。
- 能稳定区分表达式求值、语句执行、返回值、副作用、脚本回显和 REPL 回显。
- 能解释：`print(...)` 的屏幕输出不是调用表达式的返回值。
- 能解释：`append()`、`sort()`、`update()` 等原地修改方法通常返回 `None`。
- 能把 P2 的对象模型迁移到语句层：名字绑定对象，原地修改改变对象本体，重新绑定改变名字指向。
- 能说明物理行、逻辑行、缩进代码块、控制流进入、`def` 时间线和 `return` 控制流转移。
- 能在游戏本地化资源审计语境中解释 `dry_run`、缺失键集合、早返回、输出副作用和日志文件副作用边界。

❗ 不确定 / 模糊：
- 后续 C11 要继续防止把“赋值语句的执行效果”说成“赋值语句返回值”。
- 需要系统区分：赋值绑定新对象、增强赋值可能原地修改、方法调用可能修改对象并返回 `None`。
- 需要压实：链式赋值会让多个名字绑定同一个右侧对象，若右侧是可变对象会产生共享引用风险。
- 需要系统学习：序列解包、星号解包、嵌套解包和目标数量不匹配时的错误。
- 需要注意：属性赋值、下标赋值和切片赋值可能触发对象协议，不只是“给变量改值”。
- 需要继续把输出流当成工程边界：控制台输出、文件输出、日志 handler、缓冲刷新、编码和失败路径都要分层。

❌ 卡住的问题：
- 暂无明确卡住问题。
- 预计可能踩坑：
  1. 把 `x = expr` 误说成“返回 expr 的值”；
  2. 把 `a = b = []` 误认为创建两个独立列表；
  3. 把 `+=` 一律等同于 `x = x + y`；
  4. 把 `items.append(x)` 的返回值当成修改后的列表；
  5. 把 `print()` 的输出当成返回值；
  6. 把脚本裸表达式和 REPL 回显混为一谈；
  7. 忽略 `print(..., file=...)` 会把输出副作用转移到文件对象或其它流对象。

---

【当前小阶段目标】
📘 学习目标：
- 系统掌握 Python 赋值语句的语义：右侧先求值，左侧目标再绑定、写入或解包。
- 理解名字赋值、属性赋值、下标赋值、切片赋值和解包赋值的共同点与差异。
- 掌握链式赋值、多目标赋值、增强赋值和星号解包的核心规则。
- 继续巩固表达式语句：表达式会求值，但脚本中结果通常不会自动显示。
- 精确理解 `print()`：它是函数调用表达式，主要价值通常是输出副作用，返回值通常是 `None`。

🧠 理解深度：
- 保底：能读懂基础赋值、链式赋值、解包赋值和 `print()` 输出。
- 最好：面对任意 C11 范围代码片段，能准确判断：
  1. 右侧表达式何时求值；
  2. 左侧目标是名字、属性、下标、切片还是解包结构；
  3. 语句改变的是名字绑定、容器内容、对象属性还是输出流；
  4. 增强赋值是原地修改还是创建新对象再绑定；
  5. 多个名字是否共享同一个可变对象；
  6. `print()` 写向哪个流，返回值是否被使用；
  7. 哪些写法在工程中清晰，哪些写法虽然合法但不推荐。

🛠 实践目标：
- 在 `practice/P3_Statements_and_Syntax/C11_Assignments_Expressions_and_Prints/`
  下建立 README 和可运行的小实验脚本。
- 小实验应覆盖：
  1. 普通赋值、链式赋值和共享引用；
  2. 序列解包、星号解包和嵌套解包；
  3. 增强赋值在 list、tuple、str、dict/set 等对象上的差异；
  4. 属性赋值、下标赋值和切片赋值；
  5. 表达式语句在脚本中的结果丢弃；
  6. `print()` 的 `sep`、`end`、`file`、`flush` 参数；
  7. 一个面向本地化资源审计的小型输出实验，例如把缺失 key 摘要输出到控制台、`StringIO` 或文本文件。

---

【你回答时的要求】
- 最高优先级：先读取项目根目录 `AGENTS.md`、
  `docs/PYTHON_LEARNING_ROADMAP.md`、`notes/Python_Learning_Profile.md`、
  `notes/P3_Statements_and_Syntax.md` 和本启动模板，再使用
  `$pythonpractice-learning-stage` 工作流推进。
- 当前新会话只正式推进 `C11_Assignments_Expressions_and_Prints`。
- 如果项目中已经存在同主题 C11 文件，请把本启动模板作为唯一正式入口；
  既有同主题文件只作为背景材料，不要让它们覆盖本会话节奏。
- 指出我理解中的漏洞，哪怕偏差很小也要显式指出并纠正。
- 不要假设我提出的表述必然正确。若我混淆“表达式”“语句”“返回值”
  “副作用”“输出”“回显”“名字绑定”“原地修改”“重新绑定”，请立即指出。
- 优先讲本质，尤其是：
  - 右侧表达式求值 vs 左侧赋值目标处理；
  - 名字绑定 vs 对象原地修改；
  - 链式赋值共享对象 vs 创建独立对象；
  - 增强赋值的原地修改机会 vs 重新绑定结果；
  - 表达式语句求值 vs 脚本自动显示；
  - `print()` 输出副作用 vs `print()` 返回值；
  - 输出流、文件对象、日志和外部副作用边界。
- 多用可运行的小例子拆解，并适当安排预测题、小实验和阶段总结。
- 尽量结合工程实践，尤其是 CLI 输出、日志、JSON/CSV 批处理、游戏本地化
  资源筛选、缺失 key 摘要、规则执行结果和报告输出。
- 如果需要修改仓库文件，请小范围、可验证地修改，并在修改后运行合适的验证。
- 不要把 `tests/` 目录默认当作成熟测试套件，也不要随意操作其下文件。
- 不要因为教学脚本之间存在局部重复就急于提取仓库级公共 `src/` 包。
- 小阶段收束时，按职责审计受影响的长期文档；用户级 Codex 记忆只在我明确
  要求时同步。

---

【补充】
- 当前操作系统：
  Windows 11
- 当前项目路径：
  `D:\MySoftwareDownload\PythonPractice\LearningPython5E`
- 当前学习画像：
  `D:\MySoftwareDownload\PythonPractice\LearningPython5E\notes\Python_Learning_Profile.md`
- 当前主要开发工具：PyCharm `2023.3.5`，暂不升级。项目 SDK 已指向
  `.venv-py314\Scripts\python.exe`，但旧 PyCharm 会把它显示为
  `Python 3.10 (LearningPython5E)`；实际版本以 `sys.version` 和
  `sys.executable` 为准。
- 当前日常学习环境：Python `3.14.5` 的 `.venv-py314`。
- 每次新开 PowerShell 会话后，先在项目根目录运行：

      .\.venv-py314\Scripts\Activate.ps1
      python --version

  预期版本为 Python `3.14.5`。
- 未激活虚拟环境时，裸 `python` 仍指向历史解释器 Python `3.9.13`；
  `py` 与 `py -3.14` 指向 Python `3.14.5`。旧 `.venv` 仅用于历史回归。
- 新 Python `3.14.5` 与 `.venv-py314` 不需要全局 `sitecustomize.py`。
- 学习风格：
  - 我希望你像长期学习伙伴一样推进，不只回答问题，也帮助我发现盲区、
    安排下一步并形成阶段性成果。
  - 回答质量和理解深度比速度更重要。
  - 我希望保留可复盘的笔记、脚本、测验、批改记录和阶段项目。
</Contents>
```
