# C12 if Tests and Syntax Rules Startup Template

下面的内容用于新开会话，正式进入
`P3_Statements_and_Syntax / C12_if_Tests_and_Syntax_Rules`。它不是 C11 的
继续讲解，也不是一次性展开整个 P3。新会话应只正式推进 C12；C13 的循环、
C14 的迭代和后续函数专题只作为必要预告出现。

```text
<Subject>
当前新开会话的启动模板（可复用）：条件判断、真值测试与语法规则：短路求值、比较链和控制流选择
</Subject>

<Contents>
【阶段名称】
当前大阶段：学习 Python 的语句和语法（Statements and Syntax）

已完成的大阶段：
1. `P1_Getting_Started`（已完成）
2. `P2_Types_and_Operations`（已完成并通过收束验收）

已完成的小阶段：
1. `C10_Introducing_Python_Statements`（已完成，阶段小测建议得分 `96 / 100`）
2. `C11_Assignments_Expressions_and_Prints`（已完成，阶段测验建议得分 `100 / 100`）

当前小阶段：`C12_if_Tests_and_Syntax_Rules`

主要内容包括但不限于：
`if` / `elif` / `else` 的控制流选择；条件表达式先求值、`if` 再进行真值测试；
Python 对象的真值规则；`bool()`；`__bool__()` 与 `__len__()` 协议；数值零、
空容器、空字符串和 `None` 的假值边界；`and` / `or` 的短路求值和返回操作数
对象；`not` 返回布尔对象；比较运算；比较链的求值顺序；`==` 相等性与 `is`
身份；成员测试；条件表达式 `x if condition else y`；缩进代码块和语法规则；
必要的 `match` / `case` 适用边界预告。

本小阶段要把 C11 已经稳定掌握的“表达式求值 -> 语句处理目标/副作用”模型推进到
条件控制流：先判断哪些表达式真正被求值，再判断条件结果如何参与真值测试，最后
判断控制流进入哪个代码块。C13 的循环只做必要连接，不提前系统展开。

---

【我当前的位置】
✔ 已掌握：
- 已通过 `C10_Introducing_Python_Statements`，阶段小测建议得分 `96 / 100`。
- 已通过 `C11_Assignments_Expressions_and_Prints`，阶段测验建议得分 `100 / 100`。
- 能稳定区分表达式求值、语句执行、返回值、副作用、脚本回显和 REPL 回显。
- 能稳定解释名字绑定、对象原地修改、重新绑定、共享引用和对象身份。
- 能分析普通赋值、链式赋值、解包、属性/下标/切片目标和增强赋值。
- 能解释增强赋值不是事务式操作：原地修改可能在写回失败前已经发生。
- 能准确区分 `print()` 返回的 `None` 与 stdout、stderr、`StringIO`、文件流中的
  输出副作用。
- 能在本地化审计函数中分离结构化返回数据和可选人读输出。
- 当前能力判断为：准中级入门已经基本坐稳，正在稳定向可独立完成小型工程设计
  的中级入门过渡。

❗ 不确定 / 模糊：
- 需要系统压实：`if x:` 不是在判断 `x == True`，而是对对象 `x` 进行真值测试。
- 需要系统学习：内置对象的真假规则，以及自定义对象如何通过 `__bool__()` 或
  `__len__()` 参与真值测试。
- 需要防止把 `and` / `or` 一律说成返回 `True` 或 `False`；它们通常返回某个
  操作数对象。
- 需要压实短路求值：未被求值的操作数表达式不会产生调用、副作用或异常。
- 需要区分 `not x` 的布尔返回值与 `and` / `or` 的操作数返回值。
- 需要系统理解比较链，例如 `low < value <= high`，包括中间表达式的求值次数。
- 需要继续警惕把 `==` 相等性说成“同一个对象”，或用 `is` 比较普通业务值。
- 需要区分条件表达式与 `if` 语句：前者产生一个值，后者组织控制流代码块。
- 需要判断 `if` / `elif` 中只有第一个命中的分支执行，后续条件可能根本不求值。
- 需要把 `match` 当作结构化模式匹配工具，而不是机械替代所有 `if` / `elif`。

❌ 卡住的问题：
- 暂无明确卡住问题。
- 预计可能踩坑：
  1. 把 `if x:` 误说成 `if x == True:`；
  2. 把所有非空对象都无条件说成真，而忽略自定义真值协议；
  3. 把 `a and b`、`a or b` 的结果一律预测成 `bool`；
  4. 忽略短路后右侧表达式没有求值，因此相应副作用没有发生；
  5. 把 `not x` 与 `x is False` 混为一谈；
  6. 把 `a < b < c` 简单当成会重复求值 `b` 的文本替换；
  7. 在普通字符串、数字等业务值比较中滥用 `is`；
  8. 把条件表达式当成缩写版代码块，塞入复杂副作用；
  9. 认为多个 `if` 与 `if` / `elif` 的控制流效果相同；
  10. 把 `match` 当作支持自动 fall-through 的 C 风格 `switch`。

---

【当前小阶段目标】
📘 学习目标：
- 系统掌握 `if` / `elif` / `else` 的条件求值、分支选择和代码块执行规则。
- 理解 Python 真值测试协议，而不是只背“空是假、非空是真”的表面结论。
- 掌握 `and` / `or` 的短路顺序及返回操作数对象的语义。
- 掌握 `not`、比较运算、成员测试和比较链的核心规则。
- 区分 `==`、`!=`、`is`、`is not` 和 `in` / `not in` 的职责。
- 理解条件表达式产生值，而 `if` 语句组织控制流。
- 掌握 Python 缩进代码块和相关语法可读性规则。
- 理解 `match` / `case` 适合结构化模式分派，不应无条件替代 `if`。

🧠 理解深度：
- 保底：能根据常见对象真假正确预测 `if` 分支，并写出清晰的
  `if` / `elif` / `else`。
- 最好：面对任意 C12 范围代码片段，能准确判断：
  1. 哪些条件表达式按什么顺序求值；
  2. 哪些表达式因短路或前序分支命中而没有求值；
  3. 对象通过什么规则得到真值结果；
  4. `and` / `or` 最终返回哪个操作数对象；
  5. `not`、比较和成员测试返回什么对象；
  6. 比较链中间表达式求值几次；
  7. 控制流进入哪个代码块，产生哪些绑定、修改或外部副作用；
  8. 哪些条件写法虽然合法但会隐藏副作用、重复求值或降低可读性。

🛠 实践目标：
- 在 `practice/P3_Statements_and_Syntax/C12_if_Tests_and_Syntax_Rules/`
  下建立 README 和可运行的小实验脚本。
- 小实验应覆盖：
  1. 数值、字符串、容器、`None` 和自定义对象的真值测试；
  2. `__bool__()` / `__len__()` 的调用与错误边界；
  3. `and` / `or` 的操作数返回和短路副作用；
  4. `not`、比较、身份与成员测试；
  5. 比较链与中间表达式求值次数；
  6. `if` / `elif` / `else` 与多个独立 `if` 的差异；
  7. 条件表达式的值、求值分支和可读性边界；
  8. 一个本地化规则决策实验，例如根据 `strict`、`dry_run`、缺失 key、空译文、
     输出模式和严重级别选择报告路径；
  9. 一个小型 `match` 对照实验，只用于说明适用边界。

---

【你回答时的要求】
- 最高优先级：先读取项目根目录 `AGENTS.md`、
  `docs/PYTHON_LEARNING_ROADMAP.md`、`notes/Python_Learning_Profile.md`、
  `notes/P3_Statements_and_Syntax.md` 和本启动模板，再使用
  `$pythonpractice-learning-stage` 工作流推进。
- 当前新会话只正式推进 `C12_if_Tests_and_Syntax_Rules`。
- 如果项目中已经存在同主题 C12 文件，请把本启动模板作为唯一正式教学入口；
  既有同主题文件只作为背景材料，不要让它们覆盖本会话节奏。
- 指出我理解中的漏洞，哪怕偏差很小也要显式指出并纠正。
- 不要假设我提出的表述必然正确。若我混淆“条件表达式求值”“真值测试”
  “布尔对象”“操作数返回”“短路”“相等性”“身份”“代码块执行”“副作用”，
  请立即指出。
- 优先讲本质，尤其是：
  - 对象本体 vs 对象的真值测试结果；
  - `if x` vs `x == True`；
  - `and` / `or` 短路顺序 vs 最终返回的操作数对象；
  - `not` 返回 `bool` vs `and` / `or` 可能返回任意对象；
  - `==` 相等性 vs `is` 身份；
  - 比较链的求值顺序 vs 简单文本改写；
  - 条件表达式产生值 vs `if` 语句选择代码块；
  - 条件未求值或代码块未进入时，相应副作用不会发生；
  - `match` 的结构化模式分派边界。
- 多用可运行的小例子拆解，并适当安排预测题、小实验和阶段总结。
- 尽量结合工程实践，尤其是 CLI 开关、日志级别、输入校验、本地化资源筛选、
  缺失 key、空译文、严格模式、dry-run、报告输出模式和失败路径。
- 如果需要修改仓库文件，请小范围、可验证地修改，并在修改后运行合适的验证。
- 不要把 `tests/` 目录默认当作成熟测试套件，也不要操作其下文件。
- 不要因为教学脚本之间存在局部重复就急于提取仓库级公共 `src/` 包。
- 小阶段收束时，按职责审计受影响的长期文档；用户级 Codex 记忆只在我明确
  要求时同步。

---

【补充】
- 当前操作系统：Windows 11。
- 当前项目路径：
  `D:\MySoftwareDownload\PythonPractice\LearningPython5E`
- 当前学习画像：
  `D:\MySoftwareDownload\PythonPractice\LearningPython5E\notes\Python_Learning_Profile.md`
- 当前 P3 阶段笔记：
  `D:\MySoftwareDownload\PythonPractice\LearningPython5E\notes\P3_Statements_and_Syntax.md`
- 当前主要开发工具：PyCharm `2023.3.5`，暂不升级。项目 SDK 已指向
  `.venv-py314\Scripts\python.exe`，但旧 PyCharm 会把它显示为
  `Python 3.10 (LearningPython5E)`；实际版本以 `sys.version` 和
  `sys.executable` 为准。
- 当前日常学习环境：Python `3.14.5` 的 `.venv-py314`。
- 每次新开 PowerShell 会话后，先在项目根目录运行：

      .\.venv-py314\Scripts\Activate.ps1
      python --version

  预期版本为 Python `3.14.5`。
- 未激活虚拟环境时，裸 `python` 仍指向历史解释器 Python `3.9.13`；
  `py` 与 `py -3.14` 指向 Python `3.14.5`。旧 `.venv` 仅用于历史回归。
- 新 Python `3.14.5` 与 `.venv-py314` 不需要全局 `sitecustomize.py`。
- 学习风格：
  - 我希望你像长期学习伙伴一样推进，不只回答问题，也帮助我发现盲区、
    安排下一步并形成阶段性成果。
  - 回答质量和理解深度比速度更重要。
  - 我希望保留可复盘的笔记、脚本、测验、批改记录和阶段项目。
</Contents>
```
