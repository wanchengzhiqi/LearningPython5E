# P3 Statements and Syntax Startup Template

下面的内容用于新开会话，正式进入 `P3_Statements_and_Syntax`。它不是中断恢复
模板，也不是要求一次性讲完 P3 的全部主题。新会话应从
`C10_Introducing_Python_Statements` 开始推进，并且只正式推进 C10。完成
C10 收束后，应为 C11 生成新的启动模板并另开会话。

```text
<Subject>
当前新开会话的启动模板（可复用）：Python 语句导论：表达式、语句、代码块与执行顺序
</Subject>

<Contents>
【阶段名称】
当前大阶段：学习 Python 的语句和语法（Statements and Syntax）

已完成的大阶段：
1. `P1_Getting_Started`（已完成）
2. `P2_Types_and_Operations`（已完成并通过收束验收）

当前大阶段计划包含：
1. `C10_Introducing_Python_Statements`
2. `C11_Assignments_Expressions_and_Prints`
3. `C12_if_Tests_and_Syntax_Rules`
4. `C13_while_and_for_Loops`
5. `C14_Iterations_and_Comprehensions`
6. `C15_The_Documentation_Interlude`

当前小阶段：Python 语句导论：表达式、语句、代码块与执行顺序

主要内容包括但不限于：
表达式（expression）与语句（statement）的区别；表达式求值；表达式语句；
简单语句与复合语句的初步区分；函数调用表达式与输出副作用；Python 3 中
`print()` 是函数调用而不是 `print` 语句；源码的物理行与逻辑行；括号内隐式
续行；反斜杠显式续行及其脆弱性；分号允许但通常不推荐；冒号与缩进代码块；
缩进不是装饰性的排版，而是语法结构；顶层代码的顺序执行；代码块只在控制流
进入时执行；对象模型如何延伸到语句层；注释、文档字符串与普通字符串表达式
的初步区分。

本小阶段要为后续赋值、条件、循环、迭代和推导式建立语句层模型，但不要提前
把后续章节一次性灌入。遇到赋值、真值测试或循环时，可以做必要预告，再在对应
小阶段系统展开。

---

【我当前的位置】
✔ 已掌握：
- 已经知道：Python 中一切数据以对象形式存在，类型属于对象而不是变量名。
- 已经理解：名字绑定对象；赋值通常改变绑定；原地修改改变对象。
- 已经理解：`is` 比较身份，`==` 比较相等性，普通值不要用 `is` 比较。
- 已完成数字、字符串、列表、元组、字典、集合和文件对象的系统学习。
- 能区分源码形式、对象本体、显示形式、`repr()`、`str()`、`print()`、
  交互式回显、Unicode 码位和 UTF-8 字节。
- 能解释列表、字典等可变对象的原地修改；也能解释浅拷贝、深拷贝和共享引用。
- 能解释字典键的可哈希要求，以及 `hash()` 与 `==` 如何共同影响查找和去重。
- 能把路径、文件对象、文本 `str`、字节 `bytes`、编码、JSON 文本、Python
  内存对象和磁盘字节分层说明。
- 已完成 `P2_Types_and_Operations` 阶段综合项目
  `localization_resource_auditor`，并完成旧项目 `myimporter_system` 的审查、
  改良、归档和 Python `3.14.5` 命令行回归。
- 当前 Python 基础处于：
  “准中级入门已经基本坐稳，正在向可独立做小型工程设计的中级入门过渡。”

❗ 不确定 / 模糊：
- 目前虽然已经频繁使用表达式、赋值、函数调用、条件和循环，但尚未从“语句
  如何组织程序执行”的角度系统学习。
- 需要精确区分：表达式的值、表达式求值造成的副作用、表达式语句整体的作用。
- 需要继续压实：`print(value)` 会产生输出副作用，但调用表达式本身的返回值
  通常是 `None`。
- 需要建立：物理行、逻辑行、括号内续行、显式反斜杠续行、分号和缩进代码块
  之间的关系。
- 需要解释：缩进代码块是源码语法结构，不是运行时创建的列表、字典或其它
  容器对象。
- 后续仍需系统学习：赋值语句、增强赋值、真值测试、短路求值、`match`、
  `while`、`for`、迭代器、推导式和生成器表达式。

❌ 卡住的问题：
- 暂无明确卡住问题。
- 预计可能出现困难的地方：
  1. 把“表达式会产生值”误说成“所有语句都有返回值”；
  2. 混淆交互式环境自动回显表达式值与脚本执行时的行为；
  3. 把 `print()` 的屏幕输出误当成调用表达式返回值；
  4. 把缩进理解成代码美化，而不是 Python 语法的一部分；
  5. 把代码块当作运行时容器对象；
  6. 只凭源码书写顺序解释程序，而忽略控制流会决定某个代码块是否执行；
  7. 在尚未激活新版虚拟环境时，误用裸 `python` 启动旧 Python `3.9.13`。

---

【当前小阶段目标】
📘 学习目标：
- 主要：系统建立 Python 的语句层模型，能区分表达式、语句、代码块和执行顺序。
- 次要：把 P2 已经形成的对象模型迁移到“程序如何决定下一步执行什么”。
- 进阶：为后续赋值、条件、循环、迭代和推导式建立可复用的分析框架。

🧠 理解深度：
- 保底：能解释表达式与语句的区别，能读懂缩进代码块和基本顺序执行。
- 最好：面对任意基础代码片段，能准确判断：
  1. 源码中哪些部分是表达式，哪些部分是语句；
  2. 表达式求值产生了什么对象或返回值；
  3. 哪些行为只是求值得到对象，哪些行为还产生副作用；
  4. 哪些语句按顺序执行，哪些代码块要由控制流决定是否进入；
  5. 源码换行是在结束逻辑行，还是只是在括号内继续书写；
  6. 缩进表达了什么语法归属关系；
  7. 交互式回显、`print()` 输出和函数返回值分别属于哪一层。

🛠 实践目标：
- 在 `practice/P3_Statements_and_Syntax/C10_Introducing_Python_Statements/`
  下建立可运行的小实验脚本和 README。
- 小实验应覆盖：表达式与表达式语句、返回值与副作用、脚本执行与交互式回显、
  逻辑行与物理行、缩进代码块、顺序执行和最小控制流预告。
- 当前小阶段不急于建立大型项目。整个 P3 收束时，再设计一个可配置的游戏
  本地化批处理工作流，用条件、循环、迭代和推导式组织资源筛选、规则执行和
  摘要输出。

---

【你回答时的要求】
- 最高优先级：先读取项目根目录 `AGENTS.md`、
  `docs/PYTHON_LEARNING_ROADMAP.md`、`notes/Python_Learning_Profile.md` 和
  本启动模板，再使用 `$pythonpractice-learning-stage` 工作流推进。
- 当前会话只正式推进 `C10_Introducing_Python_Statements`。C10 收束后，
  为 C11 生成新的启动模板并建议另开会话，不要在当前会话继续系统讲解 C11。
- 指出我理解中的漏洞，哪怕偏差很小也要显式指出并纠正。
- 不要假设我提出的表述必然正确。若我混淆“表达式”“语句”“返回值”
  “副作用”“输出”“回显”“代码块”“对象”，请立即指出。
- 优先讲本质，尤其是：
  - 表达式求值 vs 语句执行；
  - 表达式的值 vs 副作用；
  - `print()` 输出 vs `print()` 返回值；
  - 脚本执行 vs 交互式回显；
  - 物理行 vs 逻辑行；
  - 源码缩进结构 vs 运行时对象；
  - 顺序执行 vs 控制流选择；
  - P2 对象模型如何延伸到 P3 语句层。
- 多用可运行的小例子拆解，并适当安排预测题、小实验和阶段总结。
- 尽量结合工程实践，尤其是 CLI 参数、日志、JSON/CSV 批处理、游戏本地化
  资源筛选、规则执行和报告输出。
- 如果需要修改仓库文件，请小范围、可验证地修改，并在修改后运行合适的验证。
- 不要把 `tests/` 目录默认当作成熟测试套件，也不要随意操作其下文件。
- 不要因为教学脚本之间存在局部重复就急于提取仓库级公共 `src/` 包。
- 小阶段收束时，按职责审计受影响的长期文档；用户级 Codex 记忆只在我明确
  要求时同步。

---

【补充】
- 当前项目路径：
  `D:\MySoftwareDownload\PythonPractice\LearningPython5E`
- 当前主要开发工具：PyCharm `2023.3.5`，暂不升级。项目 SDK 已指向
  `.venv-py314\Scripts\python.exe`，但旧 PyCharm 会把它显示为
  `Python 3.10 (LearningPython5E)`；实际版本以 `sys.version` 和
  `sys.executable` 为准。
- 当前日常学习环境：Python `3.14.5` 的 `.venv-py314`。
- 每次新开 PowerShell 会话后，先在项目根目录运行：

  ```powershell
  .\.venv-py314\Scripts\Activate.ps1
  python --version

  预期版本为 Python `3.14.5`。

- 未激活虚拟环境时，裸 `python` 仍指向历史解释器 Python `3.9.13`；
  `py` 与 `py -3.14` 指向 Python `3.14.5`。旧 `.venv` 仅用于历史回归。
- 新 Python `3.14.5` 与 `.venv-py314` 不需要全局 `sitecustomize.py`。
- 学习风格：
  - 我希望你像长期学习伙伴一样推进，不只回答问题，也帮助我发现盲区、
    安排下一步并形成阶段性成果。
  - 回答质量和理解深度比速度更重要。
  - 我希望保留可复盘的笔记、脚本、测验、批改记录和阶段项目。
</Contents>
```
