# Python 对象文档卡片

## 1. 基本身份

- 卡片名称：
- 调查对象：
- 运行时类型：
- 所属模块：
- Python 版本：
- 解释器路径：

## 2. 自动诊断结果

- `repr()` 摘要：
- `callable()`：
- `inspect.signature()`：
- `inspect.getdoc()` 摘要：
- 关键 `dir()` 名称：
- 关键静态成员：

## 3. 官方契约

- 官方文档类别：
- 正式签名：
- 输入契约：
- 返回对象：
- 返回值类型：
- 是否惰性：
- 是否有副作用：
- 是否消费上游：
- 主要异常：
- 异常发生阶段：
- 版本增加或变化：
- 实现说明：

## 4. 协议判断

- 是否 iterable：
- 是否 iterator：
- `iter(obj) is obj`：
- 是否支持 `next(obj)`：
- 是否可重复遍历：
- 是否有共享状态：

## 5. 最小实验

```python
# 在这里填写最小代码
```

完整输出：

```tex
在这里填写完整输出
```

## 6. 最终结论

- 可以依赖的契约：
- 仅在 CPython 3.14.5 中观察到的行为：
- 不应依赖的显示细节：
- 推荐工程用法：
- 常见误用：
- 仍未确认的边界：
